# Screenshots & media (placeholders)

Drop real assets here before launch (see docs/adr and the main README):
- `dashboard.png` — the Forge Control Center on localhost.
- `demo.gif` — a 10–30s `/forge build me a landing page` fan-out + dashboard updating.
- `../.github/social-preview.png` — 1280×640, <1MB, solid dark bg (GitHub → Settings → Social preview).

These are intentionally not generated by the build (they need a real recorded session).

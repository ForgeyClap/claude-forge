{
  "_doc": "Forge V2 WAVE A / A1 (2026-07-18) — the SINGLE source of truth for irreversible-action and project-isolation-escape detection. Read only via .claude/forge-bin/forge-actiongate.cjs::classify() — no other file may define a parallel regex set (the exact 'regex drift' risk cc-risks/dd-autonomy flagged: forge-autonomy.cjs and forge-verify.cjs, wired in a later wave, MUST require() this same classifier rather than re-implement it). Advisory/loud, not a hook — matches this project's light-security governance (CLAUDE.md: 'no mandatory security gates'). Every level of autonomy (ask-each-phase / continue-within-mission / full-loop-until-stop) is powerless over these gates: a match means STOP and get owner_confirmed:true before proceeding, regardless of autonomy level.\n\nMATCH KINDS (2026-08-01): 'regex' matches SPOKEN INTENT against the combined free text; 'command' matches an actual COMMAND LINE per command SEGMENT (split on && || ; | & newline $( and backtick) with an optional `match.except` false-alarm valve, plus an optional `match.pattern_line` matched against the WHOLE line for a danger that only exists ACROSS a pipeline (a name-scoped `Get-Process ... | Stop-Process` / `| ForEach-Object { $_.Kill() }`, where no single segment carries both the name and the kill); 'path-escape' is the isolation check. The command gates were added because a measurement on 2026-08-01 showed the nine intent gates classified `taskkill /IM node.exe`, `Stop-Process -Name node`, `pkill node`, `rm -rf ./src` and `git reset --hard origin/main` ALL as gate:false — the first three being verbatim the commands the owner's global Orchestration-Safety HARD MUST forbids after the 2026-07-29 incident (an agent cleaning up a broken Chrome killed the live Forge gateway on port 4100 with it).\n\nWHAT THE SEGMENT SPLIT REALLY DOES — CORRECTED 2026-08-01. The first version of this file claimed the split was 'the load-bearing part' and that without it `echo hi && rm -rf /` would escape. That claim was FALSE and is retracted. Measurement (splitCommands() neutralised to return the whole string as one segment, then the corpus re-run): ZERO of the five chained cases flipped — `echo hi && rm -rf /`, `npm run dev && taskkill /IM node.exe`, `git fetch; git reset --hard origin/main`, `Get-Process node | Stop-Process` and `cd /tmp && rm -rf ./src` all still fired, because every command pattern uses `[^\\n]*` lookaheads that already scan the rest of the line. The split's real, measurable job is the OPPOSITE — FALSE-POSITIVE SUPPRESSION: it keeps each command's flags, targets and except-valve with THAT command, so `rm -rf node_modules && npm ci`, `rm -rf ./_scratch/run-1 && npm ci` and `rm ./notes.txt && echo -rf` stay silent instead of firing on a blob match. Those three are the cases that flip.\n\nTHE EXCEPT VALVE HAS NO PARSER (2026-08-01, round 4 — the rewrite). It is EXACT STRING EQUALITY between the raw command segment and 16 literal strings (command_prefixes x argument_tails). Nothing about the input is tokenised, unquoted, normalised, path-resolved or case-folded. Three earlier designs each modelled a bit more shell semantics and were each bypassed by a bit we had not modelled: substring matching (`rm -rf ./_scratch/../.claude` excused), path normalisation defeated by the segment split (`rm -rf ./_scratch/$(whoami)/../../.claude` excused), and tokenisation defeated by the PowerShell comma ARRAY (`rm -r -force .claude,./tmp` excused — one 'target' to the tokeniser, two deleted directories in a real shell). A zero-dependency classifier cannot become a complete PowerShell AND POSIX parser, so the parser was removed instead of patched a fourth time. The price is noise, and it is stated in _not_caught.exact_valve_price rather than hidden.\n\nEXAMPLES: every non-path-escape gate carries `examples: {match:[...], no_match:[...]}`. These are not documentation — forge-actiongate.test.cjs iterates them and asserts, for EVERY gate, that each `match` string really makes classify() list that gate id and each `no_match` string really does not. A gate added without examples fails the suite, so the config can never grow an unproven pattern.",
  "version": 1,
  "gates": [
    {
      "id": "deploy",
      "class": "irreversible",
      "reason": "Deploying or publishing to a live environment (or going live) is irreversible without explicit owner approval.",
      "match": {
        "kind": "regex",
        "pattern": "\\bdeploy(ed|ing|s)?\\b|\\bgo[- ]?live\\b|\\bgoing\\s+live\\b|\\b(release(d|s)?|push(ed|ing)?|roll(ed|ing)?[\\s-]*out)\\s+(it\\s+|this\\s+)?to\\s+prod(uction)?\\b|\\bship(ped|ping)?\\s+(it\\b|this\\b|(the\\s+)?(app|site|feature|build|release|update)\\b|to\\s+(users?|production|prod|customers?|clients?|live)\\b)|\\bpublish(ed|ing)?\\s+(the\\s+)?(app|site|website|webpage|homepage|package|build)\\b",
        "flags": "i"
      },
      "examples": {
        "match": [
          "let's deploy this to prod now",
          "release to prod",
          "ship it",
          "publish the website",
          "roll out to production"
        ],
        "no_match": [
          "here is the deployment architecture diagram",
          "the production build compiles",
          "product roadmap",
          "reproduce the bug"
        ]
      }
    },
    {
      "id": "git-push",
      "class": "irreversible",
      "reason": "Pushing commits to a remote (especially a force-push or a push to main/master) is irreversible without explicit owner approval.",
      "match": {
        "kind": "regex",
        "pattern": "\\bgit\\s+push\\b|\\bforce[- ]?push\\b|\\bpush\\s+(to\\s+)?(origin|remote|main|master)\\b",
        "flags": "i"
      },
      "examples": {
        "match": [
          "please git push this to origin",
          "force-push the branch",
          "push to main"
        ],
        "no_match": [
          "let's push forward with the roadmap",
          "git status"
        ]
      }
    },
    {
      "id": "spend",
      "class": "irreversible",
      "reason": "Spending money, charging a card, issuing a refund, or placing a real bet is irreversible without explicit owner approval.",
      "match": {
        "kind": "regex",
        "pattern": "\\bcharge\\s+(the\\s+)?(card|customer|client)\\b|\\bissue\\s+a\\s+refund\\b|\\bmake\\s+a\\s+payment\\b|\\bplace\\s+a\\s+(real[- ]?money\\s+)?bet\\b|\\bspend\\s+(real\\s+)?money\\b|\\bpurchase\\s+(the|a|an)\\b",
        "flags": "i"
      },
      "examples": {
        "match": [
          "go ahead and charge the customer for this order",
          "issue a refund",
          "place a real-money bet"
        ],
        "no_match": [
          "the invoice UI mockup needs a redesign",
          "explain how refunds work"
        ]
      }
    },
    {
      "id": "dns-change",
      "class": "irreversible",
      "reason": "Changing DNS records or nameservers can break production traffic and is irreversible without explicit owner approval.",
      "match": {
        "kind": "regex",
        "pattern": "\\b(change|update|point|switch|modify)\\s+(the\\s+)?dns\\b|\\bdns\\s+(record|change|update)\\b|\\bnameserver\\b|\\bcname\\s+record\\b",
        "flags": "i"
      },
      "examples": {
        "match": [
          "please change the DNS record for the domain",
          "update the nameserver",
          "add a cname record"
        ],
        "no_match": [
          "explain how DNS resolution generally works"
        ]
      }
    },
    {
      "id": "prod-activate",
      "class": "irreversible",
      "reason": "Activating a production environment or switching a system into production is irreversible without explicit owner approval.",
      "match": {
        "kind": "regex",
        "pattern": "\\bactivate\\s+(the\\s+)?prod(uction)?\\b|\\bprod(uction)?\\s+activat\\w*\\b|\\bswitch\\s+(the\\s+)?prod(uction)?\\s+on\\b",
        "flags": "i"
      },
      "examples": {
        "match": [
          "activate the production environment now",
          "switch the production on"
        ],
        "no_match": [
          "discuss the production timeline for next quarter"
        ]
      }
    },
    {
      "id": "credential-attach",
      "class": "irreversible",
      "reason": "Attaching, inserting, or hardcoding a real credential/secret/token/password is irreversible-risk (exposure) without explicit owner approval.",
      "match": {
        "kind": "regex",
        "pattern": "\\b(attach|insert|hardcode)\\s+(the\\s+)?(api[- ]?key|credential|secret|token|password)\\b",
        "flags": "i"
      },
      "examples": {
        "match": [
          "please attach the api key to this config file",
          "hardcode the token"
        ],
        "no_match": [
          "explain how credentials are validated during login"
        ]
      }
    },
    {
      "id": "credential-rotate",
      "class": "irreversible",
      "reason": "Rotating or revoking a live credential/secret/token invalidates the old one irreversibly without explicit owner approval.",
      "match": {
        "kind": "regex",
        "pattern": "\\brotate\\s+(the\\s+)?(api[- ]?key|credential|secret|token|password)\\b|\\brevoke\\s+(the\\s+)?(api[- ]?key|credential|secret|token)\\b",
        "flags": "i"
      },
      "examples": {
        "match": [
          "we need to rotate the api key immediately",
          "revoke the token"
        ],
        "no_match": [
          "read the rotation policy documentation"
        ]
      }
    },
    {
      "id": "workflow-activate",
      "class": "irreversible",
      "reason": "Activating a live n8n/automation workflow can trigger real-world side effects and is irreversible without explicit owner approval.",
      "match": {
        "kind": "regex",
        "pattern": "\\b(activate|enable|turn\\s+on)\\s+(the\\s+)?(n8n\\s+)?workflow\\b|\\blive[- ]?activat\\w*\\s+(the\\s+)?workflow\\b",
        "flags": "i"
      },
      "examples": {
        "match": [
          "please activate the n8n workflow for lead capture",
          "turn on the workflow"
        ],
        "no_match": [
          "design the workflow diagram for the automation"
        ]
      }
    },
    {
      "id": "outbound-sms",
      "class": "irreversible",
      "reason": "Sending an SMS, outbound call, or live outreach/email is irreversible without explicit owner approval (draft-only unless approved).",
      "match": {
        "kind": "regex",
        "pattern": "\\b(send|dispatch)\\s+(an?\\s+)?(sms|text\\s+message|outbound\\s+(call|message)|cold\\s*email|outreach\\s+(email|message))\\b|\\bsend(?:ing)?\\s+(?:the|an?|our|your)\\s+(?:\\w+\\s+){0,2}email\\s+to\\s+(?:the\\s+)?(client|customer|customers|lead|leads|user|users|team|prospect|prospects|owner)\\b|\\bemail(?:ed|ing)?\\s+(?:the\\s+|our\\s+|a\\s+)?(client|customer|customers|lead|leads|user|users|team|prospect|prospects|owner)\\b",
        "flags": "i"
      },
      "examples": {
        "match": [
          "please send an sms to the customer now",
          "send the email to the client",
          "email the customer"
        ],
        "no_match": [
          "review the sms template copy",
          "draft an email to the client",
          "read the email"
        ]
      }
    },
    {
      "id": "kill-by-name",
      "class": "irreversible",
      "reason": "Killing a process by IMAGE/NAME (taskkill /IM, Stop-Process -Name, pkill, killall, wmic process where name=..., or a name-scoped Get-Process piped into Stop-Process / $_.Kill()) takes down every process with that name, including unrelated live services — the owner's global HARD MUST allows only killing an exact PID you own.",
      "match": {
        "kind": "command",
        "pattern": "\\btaskkill\\b(?=[^\\n]*(?:[\\s/-]im\\b|\\bimagename\\b))|\\b(?:Stop-Process|spps|kill)\\b(?=[^\\n]*\\s-{1,2}(?:Name|ProcessName)\\b)|\\b(?:pkill|killall)\\b|\\bwmic\\b(?=[^\\n]*\\bprocess\\b)(?=[^\\n]*\\bname\\s*=)(?=[^\\n]*\\b(?:delete|terminate)\\b)",
        "pattern_line": "\\bGet-Process\\b(?![^\\n]*\\s-{1,2}(?:Id|InputObject)\\b)[^\\n]*\\|[^\\n]*(?:\\bStop-Process\\b|\\bspps\\b|\\bkill\\b|\\.Kill\\s*\\(\\s*\\))",
        "flags": "i",
        "_pattern_doc": "SEGMENT pattern: a kill that names its victim inside ONE command — taskkill /IM, an explicit -Name/-ProcessName on Stop-Process/spps/kill, pkill/killall, or a wmic name= delete|terminate. A bare `Stop-Process` no longer fires on its own segment: that was a false alarm (2026-08-01), because `Stop-Process 22420` binds positionally to -Id and `Get-Process -Id 22420 | Stop-Process` is PID-scoped — both are exactly what the HARD MUST ALLOWS, yet both fired.",
        "_pattern_line_doc": "LINE pattern: the danger that no single segment can see. `Get-Process node | Stop-Process` and `Get-Process node | ForEach-Object { $_.Kill() }` split into a harmless selector and a target-less killer; only the whole line shows that the selection was BY NAME. The negative lookahead keeps `Get-Process -Id 22420 | Stop-Process` silent — the selection there is by PID."
      },
      "examples": {
        "match": [
          "taskkill /IM node.exe",
          "taskkill /F /T /IM chrome.exe",
          "taskkill /fi \"IMAGENAME eq node.exe\"",
          "Stop-Process -Name node",
          "Stop-Process  -Name  chrome -Force",
          "stop-process -name node",
          "Get-Process node | Stop-Process",
          "Get-Process node | ForEach-Object { $_.Kill() }",
          "Get-Process chrome | %{ $_.Kill() }",
          "wmic process where name=\"node.exe\" delete",
          "wmic process where \"name='node.exe'\" call terminate",
          "pkill node",
          "pkill -9 node",
          "killall node",
          "sudo pkill -f node",
          "kill -Name node",
          "npm run dev && taskkill /IM node.exe"
        ],
        "no_match": [
          "taskkill /PID 22420 /F",
          "Stop-Process -Id 22420",
          "Stop-Process -InputObject $p",
          "Stop-Process 22420",
          "Get-Process -Id 22420 | Stop-Process",
          "Get-Process -Id 22420 | Stop-Process -Force",
          "wmic process where processid=22420 delete",
          "wmic process get name",
          "kill 1234",
          "kill -9 1234",
          "Get-Process node",
          "add a dark-mode toggle to the settings page"
        ]
      }
    },
    {
      "id": "destructive-delete",
      "class": "irreversible",
      "reason": "A recursive, forced delete (rm -rf, Remove-Item -Recurse, and their Windows aliases del/erase/rd/rmdir/ri, plus rmdir /s, del /s, rimraf) destroys a whole tree with no undo — confirm the exact target with the owner first. `rm` is itself a Remove-Item alias on Windows, so `rm -Recurse .claude` is caught too (round 6); the two-letter `rm -r` is NOT, because it is also POSIX's ordinary recursive flag and is pinned as a false alarm — see _not_caught.alias_arm_asymmetry. A recursive listing piped into a delete (Get-ChildItem -Recurse | Remove-Item) is the same destruction spread over two commands and is caught by match.pattern_line. Only 16 exact command strings (a plain recursive delete of node_modules or _scratch — see match.except._allowed_segments) are excused as ordinary work; anything else, including every variant of them, warns.",
      "match": {
        "kind": "command",
        "pattern": "\\b(?:rm|del|erase|rd|rmdir)\\b(?=[^\\n]*\\s(?:-(?!-)[A-Za-z]*r[A-Za-z]*\\b|--recursive\\b))(?=[^\\n]*\\s(?:-(?!-)[A-Za-z]*f[A-Za-z]*\\b|--force\\b))|\\b(?:Remove-Item|ri|del|erase|rd|rmdir)\\b(?=[^\\n]*\\s-{1,2}(?:r|re|rec|recu|recur|recurs|recurse)\\b)|\\brm\\b(?=[^\\n]*\\s-{1,2}(?:re|rec|recu|recur|recurs|recurse)\\b)|\\b(?:rmdir|rd)\\b(?=[^\\n]*\\s/s\\b)|\\b(?:del|erase)\\b(?=[^\\n]*\\s/s\\b)|^\\s*(?:(?:sudo|npx)\\s+|(?:pnpm|yarn)\\s+dlx\\s+|-{1,2}[A-Za-z][A-Za-z0-9-]*\\s+)*rimraf\\b",
        "pattern_line": "\\b(?:Get-ChildItem|gci|ls|dir)\\b[^|\\n]*\\s-{1,2}(?:r|re|rec|recu|recur|recurs|recurse)\\b[^|\\n]*\\|[^\\n]*(?:\\b(?:Remove-Item|ri|rm|del|erase|rd|rmdir)\\b|\\.Delete\\s*\\(\\s*\\))",
        "flags": "i",
        "_pattern_doc": "SIX ARMS. (1) The POSIX-BUNDLE arm requires a REAL recursive flag AND a REAL force flag: a single-dash bundle containing the letter (-r/-rf/-fr/-Rf) or the long form (--recursive/--force). The previous form asked only for 'some flag containing r' and 'some flag containing f' anywhere on the line, which made `rm --force ./notes.txt` and `rm -f --verbose ./notes.txt` fire as tree destruction — a false alarm on a single-file delete (measured 2026-08-01). (2) The POWERSHELL-RECURSE arm accepts every unambiguous abbreviation of -Recurse (-r, -re, -rec, ... ) because `Remove-Item -r -Force ./src` is a real, fully-effective recursive delete that the -Recurse-only form missed. (3) The RM-RECURSE arm, added in round 6 — see below. (4)+(5) The DOS arms (`rd /s`, `del /s`) are unchanged. (6) The rimraf arm is anchored to the START of the segment (optionally behind sudo/npx/pnpm dlx/yarn dlx) so `npm install rimraf --save-dev` does not fire. That arm ended in `rimraf\\s+\\S` until 2026-08-01 round 3: it required a visible target, so ``npx rimraf `cat list`ature`` split into the target-less segment `npx rimraf` and fired NOTHING. Every other arm still matches its own flags-only stump (`rm -rf`, `Remove-Item -Recurse -Force`, `rmdir /s /q`), so rimraf was the one arm an amputation could mute. It is now `rimraf\\b`; the price is that a bare, argument-less `rimraf` (a no-op nobody types) warns.\n\nROUND 5, FAMILY 1 (2026-08-01) — THE ALIAS HOLE. `Get-Alias del,erase,rd,rmdir,ri,rm` on this machine returns Remove-Item for ALL SIX. Arms 1 and 2 covered only `rm`/`Remove-Item`/`ri`; `del`, `erase`, `rd` and `rmdir` appeared ONLY in the DOS arms, which demand a `/s` switch. So every attack in the examples below fired with `rm` and went silent the moment you wrote `del` — measured: 60 of 60 alias-substituted attacks escaped, including the round-3 substitution attack and the round-4 comma array. The four names are now in BOTH arm 1 and arm 2, so `del -Recurse -Force .claude`, `rd -r -force ./src` and `rmdir -rf ./_scratch/$(whoami)/../../.claude` all fire, while the DOS forms (`rd /s /q dist`, `del /f /s /q C:\\src`) keep firing through arms 3-4 and the bare forms (`del notes.txt`, `rd emptydir`) stay silent.\n\nDELIBERATE ASYMMETRY: `Remove-Item` and `ri` are in arm 2 but NOT arm 1. Arm 1's force test accepts any single-dash flag containing an 'f', and PowerShell's `-Force` contains BOTH an 'r' and an 'f' — so putting the spelled-out cmdlet into arm 1 would fire on `Remove-Item -Force ./notes.txt`, a single-file delete pinned as a no_match since round 4. The four alias names have no such pinned case, so they carry the wider arm. The cost is stated in _not_caught.alias_arm_asymmetry rather than hidden.\n\nROUND 6 (2026-08-01) — THE ALIAS HOLE HAD SWAPPED DIRECTION, and arm 3 is the fix. Round 5 put `del`/`erase`/`rd`/`rmdir` into BOTH arms but left `rm` in arm 1 only, and round 5's own `_not_caught` text then claimed all five were 'in both'. They were not: `rm` is a Remove-Item alias on this machine exactly like the other four (`Get-Alias rm` -> Remove-Item, run 2026-08-01), so `rm -Recurse .claude` matched arm 1 on neither test — `-Recurse` carries no 'f' — and arm 2 did not list `rm`. MEASURED SILENT: `rm -Recurse .claude`, `rm -recurse ./src`, `rm -rec .claude`, `rm -re .claude`. Arm 3 is arm 2 restricted to `rm` with the abbreviation set STARTING AT `-re`: the bare `-r` is deliberately excluded, because `rm -r ./emptydir` has been a pinned no_match since round 4 and `rm -r` is POSIX's ordinary recursive flag — the two-letter form is genuinely ambiguous between the two shells and admitting it would break that pin. `--recursive` is excluded too, so that the everyday `aws s3 rm s3://bucket --recursive` does not start warning. Both exclusions leave a real, measured gap and both are written down in _not_caught.non_recursive_deletes rather than hidden.\n\nROUND 5, FAMILY 2 — THE PIPELINE HOLE (match.pattern_line). `Get-ChildItem .claude -Recurse | Remove-Item -Force` splits into a harmless recursive LISTING and a target-less non-recursive DELETE: neither segment is a recursive forced delete, so no segment arm could ever see it — measured, 5 of 5 pipeline forms escaped. pattern_line is modelled on kill-by-name's: it matches the WHOLE line, requires the recursion flag to sit on the LEFT of the pipe (`[^|\\n]*` keeps it there) and a delete verb or a `.Delete()` call on the right. It is never segment-split and has no except valve, exactly like kill-by-name's.",
        "except": {
          "kind": "exact-segment",
          "command_prefixes": [
            "rm -rf",
            "Remove-Item -Recurse -Force",
            "npx rimraf",
            "rimraf"
          ],
          "argument_tails": [
            "node_modules",
            "./node_modules",
            "_scratch",
            "./_scratch"
          ],
          "_doc": "FALSE-ALARM VALVE — EXACT STRING EQUALITY, no parser (2026-08-01, round 4). forge-actiongate.cjs::excusedSegments() builds ONE literal set from command_prefixes x argument_tails joined by a single space (4 x 4 = 16 fixed strings, listed in _allowed_segments below), and isExcusedSegment() is a Set lookup on the RAW command segment. Nothing about the input is tokenised, unquoted, normalised, path-resolved or case-folded. A segment that is not byte-identical to one of the 16 gets the warning — including every comma, quote, metacharacter, `..` and substitution, because none of the 16 contains one. The caller consults the valve ONLY for a segment the splitter marked INTACT, so a stump such as `rm -rf node_modules` cut out of `rm -rf node_modules$(echo /../.claude)` cannot be excused either.",
          "_why_no_parser": "Three consecutive rounds of path modelling were bypassed by shell semantics we had not modelled: round 2 substring-matched the raw path (`rm -rf ./_scratch/../.claude` excused); round 3 was handed an amputated path by the segment split (`rm -rf ./_scratch/$(whoami)/../../.claude` excused); round 4 met the PowerShell comma ARRAY (`rm -r -force .claude,./tmp` excused — one 'target' to the tokeniser, two directories to PowerShell, proven end-to-end in a throwaway directory). A zero-dependency classifier will never be a complete PowerShell AND POSIX parser, so the parser was removed rather than extended a fourth time.",
          "_allowed_segments": [
            "rm -rf node_modules",
            "rm -rf ./node_modules",
            "rm -rf _scratch",
            "rm -rf ./_scratch",
            "Remove-Item -Recurse -Force node_modules",
            "Remove-Item -Recurse -Force ./node_modules",
            "Remove-Item -Recurse -Force _scratch",
            "Remove-Item -Recurse -Force ./_scratch",
            "npx rimraf node_modules",
            "npx rimraf ./node_modules",
            "npx rimraf _scratch",
            "npx rimraf ./_scratch",
            "rimraf node_modules",
            "rimraf ./node_modules",
            "rimraf _scratch",
            "rimraf ./_scratch"
          ],
          "_allowed_segments_doc": "The full expansion, written out so the coverage is readable without running code. It is DERIVED, not consumed: forge-actiongate.cjs builds the set from command_prefixes x argument_tails, and a test asserts this list is exactly that product — so it can never drift into claiming coverage the code does not have."
        }
      },
      "examples": {
        "match": [
          "rm -rf ./src",
          "rm -fr ./src",
          "rm -r -f ./src",
          "rm -f -r ./src",
          "rm --recursive --force ./src",
          "rm -rf /",
          "sudo rm -rf /var/www",
          "echo hi && rm -rf /",
          "rm -rf ./src /tmp/x",
          "git rm -r -f src",
          "Remove-Item -Recurse -Force ./src",
          "Remove-Item -Recurse ./src",
          "Remove-Item -r -Force ./src",
          "Remove-Item -rec -Force ./src",
          "rmdir /s /q C:\\build",
          "rd /s /q dist",
          "del /f /s /q C:\\src",
          "npx rimraf ./src",
          "npx --yes rimraf ./src",
          "rimraf dist",
          "rm -rf ./_scratch/../.claude",
          "rm -rf /tmp/../etc",
          "rm -rf ./temp/../src",
          "rm -rf ./node_modules/../src",
          "rm -rf ./my-node_modules-backup",
          "rm -rf ./src-temp/",
          "rm -rf .\\_scratch\\..\\.claude",
          "rm -rf \"./_scratch/../.claude\"",
          "rm -rf ../tmp/x",
          "rm -rf ./_scratch/$(whoami)/../../.claude",
          "rm -rf ./tmp/`id -un`/../../src",
          "rm -rf \"./_scratch/a|b/../../.claude\"",
          "rm -rf ./_scratch/a\\;/../../.claude",
          "rm -rf ./_scratch/x;/../../.claude",
          "rm -rf ./_scratch/a&b/../../.claude",
          "rm -rf ./_scratch/$HOME/../../.claude",
          "rm -rf ./_scratch/x $(cat targets.txt)",
          "npx rimraf ./tmp/`id -un`/../../src",
          "Remove-Item -Recurse -Force ./_scratch/$(whoami)/../../.claude",
          "Remove-Item -Recurse -Force \"$env:TEMP\\forge-x\"",
          "rm -rf %TEMP%\\forge-x",
          "rm -rf ~/tmp/forge-x",
          "rm -rf ./_scratch/*",
          "rm -rf ./_scratch/run-1; npm ci",
          "rm -r -force .claude,./tmp",
          "rm -rf .claude,./_scratch",
          "rm -rf node_modules,../.claude",
          "Remove-Item -Recurse -Force .claude,./tmp",
          "rm -rf {node_modules,../.claude}",
          "rm -rf node_modules$(echo /../.claude)",
          "rm -rf \"node_modules\"",
          "rm -rf node_modules/",
          "rm -rf ./_scratch/run-1",
          "rm -rf /tmp/forge-test-123",
          "npx --yes rimraf node_modules",
          "sudo rm -rf node_modules",
          "Remove-Item -Recurse \"C:\\Temp\\a b\"",
          "rm -rf C:/Users/EXAMPLE/AppData/Local/Temp/claude/scratchpad/x",
          "del -Recurse -Force .claude",
          "erase -Recurse -Force .claude",
          "rd -r -force ./src",
          "rmdir -Recurse -Force ./src",
          "del -rf ./src",
          "rd -rf .claude,./build",
          "erase -rf ./_scratch/$(whoami)/../../.claude",
          "rmdir -rf node_modules$(echo /../.claude)",
          "del -rf node_modules",
          "rm -Recurse .claude",
          "rm -recurse ./src",
          "rm -rec .claude",
          "rm -Recurse ./_scratch/../.claude",
          "rm -Recurse .claude,./tmp",
          "Get-ChildItem .claude -Recurse | Remove-Item -Force",
          "gci .claude -r | ri -force",
          "dir ./src -Recurse | del -Force",
          "ls .claude -Recurse | Remove-Item -Recurse -Force",
          "Get-ChildItem .claude -Recurse | ForEach-Object { Remove-Item $_ -Force }",
          "gci -r ./src | %{ ri $_ -force }",
          "Get-ChildItem ./src -Recurse | Where-Object { $_.PSIsContainer } | Remove-Item -Force",
          "Get-ChildItem .claude -Recurse | ForEach-Object { $_.Delete() }"
        ],
        "no_match": [
          "rm ./notes.txt",
          "rm -f ./notes.txt",
          "rm -r ./emptydir",
          "rm --force ./notes.txt",
          "rm -f --verbose ./notes.txt",
          "rm -f --interactive ./notes.txt",
          "docker rm -f mycontainer",
          "Remove-Item ./notes.txt",
          "Remove-Item -Force ./notes.txt",
          "del notes.txt",
          "rmdir emptydir",
          "npm install rimraf --save-dev",
          "npm run build",
          "rm -rf node_modules",
          "rm -rf ./node_modules",
          "rm -rf _scratch",
          "rm -rf ./_scratch",
          "rm -rf node_modules && npm ci",
          "rm -rf node_modules ; npm ci",
          "rm -rf node_modules;",
          "cd /repo && rm -rf node_modules",
          "npx rimraf node_modules",
          "rimraf node_modules",
          "Remove-Item -Recurse -Force ./node_modules",
          "rd emptydir",
          "erase notes.txt",
          "del /q notes.txt",
          "rmdir /q emptydir",
          "Get-ChildItem .claude -Recurse | Measure-Object",
          "Get-ChildItem .claude | Select-Object -First 5",
          "gci -r ./src | Format-Table",
          "Get-ChildItem .claude | Remove-Item -Force"
        ]
      }
    },
    {
      "id": "git-destructive",
      "class": "irreversible",
      "reason": "A git command that discards work that was never committed (reset --hard, clean -f, checkout --force, stash drop/clear) cannot be undone from the reflog — confirm with the owner before running it. Global options between `git` and the subcommand (-C <path>, -c <k=v>, --git-dir=…, --work-tree=…) do not hide it.",
      "match": {
        "kind": "command",
        "pattern": "\\bgit(?:\\s+-{1,2}[A-Za-z][A-Za-z0-9-]*(?:=\\S+)?(?:\\s+[^-\\s]\\S*)?)*\\s+reset\\b(?=[^\\n]*\\s--hard\\b)|\\bgit(?:\\s+-{1,2}[A-Za-z][A-Za-z0-9-]*(?:=\\S+)?(?:\\s+[^-\\s]\\S*)?)*\\s+clean\\b(?=[^\\n]*\\s-{1,2}[A-Za-z]*f)|\\bgit(?:\\s+-{1,2}[A-Za-z][A-Za-z0-9-]*(?:=\\S+)?(?:\\s+[^-\\s]\\S*)?)*\\s+checkout\\b(?=[^\\n]*\\s(?:-f\\b|--force\\b))|\\bgit(?:\\s+-{1,2}[A-Za-z][A-Za-z0-9-]*(?:=\\S+)?(?:\\s+[^-\\s]\\S*)?)*\\s+stash\\s+(?:drop|clear)\\b",
        "flags": "i",
        "_pattern_doc": "The repeated `(?:\\s+-<opt>(=v)?( <value>)?)*` group is git's GLOBAL-OPTION prefix. Without it the pattern required `git` to be immediately followed by the subcommand, so `git -C /repo reset --hard origin/main` — the normal form in this project, which runs git worktrees — did not fire at all (measured 2026-08-01). It is the heaviest of the misses: a worktree reset destroys uncommitted work in a directory the operator may not even be standing in."
      },
      "examples": {
        "match": [
          "git reset --hard origin/main",
          "git reset --hard",
          "git reset --hard HEAD~3",
          "git clean -fd",
          "git clean -f -d -x",
          "git checkout --force",
          "git checkout -f .",
          "git stash drop",
          "git stash clear",
          "git -C /repo reset --hard origin/main",
          "git -C ../wt-a clean -fd",
          "git -C /repo checkout -f .",
          "git -C /repo stash drop",
          "git --git-dir=/repo/.git --work-tree=/repo reset --hard",
          "git -c core.pager=cat reset --hard HEAD"
        ],
        "no_match": [
          "git reset HEAD~1",
          "git reset --soft HEAD~1",
          "git clean -n",
          "git clean --dry-run",
          "git checkout main",
          "git stash",
          "git stash pop",
          "git status",
          "git -C /repo status",
          "git -C /repo reset --soft HEAD~1",
          "git -C /repo stash pop"
        ]
      }
    },
    {
      "id": "write-outside-root",
      "class": "isolation",
      "reason": "Writing outside the current project's root breaks Forge project isolation — refuse and confirm the exact target folder with the owner before proceeding.",
      "match": {
        "kind": "path-escape"
      }
    }
  ],
  "_not_caught": {
    "_doc": "DELIBERATE, DOCUMENTED NON-COVERAGE (rewritten 2026-08-01 in round 6, after round 5's own version of this block was measured and found to contain false sentences). Naming what is out of scope is part of the contract: an unnamed gap reads as a promise the gates do not keep, and a WRONG sentence in the very document that exists to be honest is worse than a gap that is named. On BLOCKING (reconciled 2026-08-03, after the audit sweep found this sentence contradicting precedence.md tier 1, which promised the same gates \"always\" stop the run): the two statements were each half true, and an agent reading only one of them drew the wrong conclusion. The precise contract, which precedence.md now states identically: a FIRED gate always interrupts and cannot be overridden by any lower layer — not a standing rule, not a stored pref, not autonomy mode, not even an explicit current owner instruction. What these gates are NOT is a technical enforcement layer or a complete safety net: this file is a regex classifier over an action's text with the deliberate non-coverage listed below, so an action it does not recognise is simply not gated. The absence of an interrupt is never evidence that an action was safe. \"Advisory\" as used here means \"cannot physically prevent\", never \"can be ignored\".\n\nREAD THIS FIRST, BEFORE ANY LIST BELOW: this file does not describe a safety net. It describes a REGEX CLASSIFIER OVER FREE SHELL TEXT, and such a classifier is incomplete by construction — see `classifier_is_incomplete_by_construction`. Round 5 closed two named families (Windows Remove-Item aliases, pipeline deletes); round 6 closed the `rm -Recurse` half of the first one that round 5 had claimed but not delivered. None of that closes the general case, and nothing can. Treat a silent classify() as 'nothing MATCHED', never as 'this is safe'.\n\nWHAT CHANGED IN ROUND 6: (1) `alias_arm_asymmetry` said `rm` was in both delete arms — it was not, and `rm -Recurse .claude` was silent as a result; the arm was added and the sentence rewritten. (2) `non_recursive_deletes` justified its silences as 'single/interactive deletes, not tree destruction' while covering `rm -r ./src`, which is exactly tree destruction; rewritten and split into a deliberate half and a gap half. (3) `expansion_only_at_runtime` and `pipeline_deletes_only_partly_covered` each claimed something was uncovered that measurably fires; both corrected. (4) Every remaining assertion was re-read against the live pattern and is now pinned by `_claim_probes`.",
    "_contract": "This block is BOUND TO THE GATE LIST AND TO THE CLASSIFIER, in four directions, by forge-actiongate.test.cjs sections 2e and 2f. (1) `_gate_coverage` must contain exactly one entry per id in `gates` — add a gate without writing down what it does NOT catch and the suite fails; delete a gate and leave a stale entry and the suite fails too. (2) Each entry must be a real, distinct sentence (length floor + duplicate check + a weasel-word ban), so the requirement cannot be satisfied with 'n/a'. (3) The general-limit topics are pinned by name AND by a required concrete example, so nobody can dilute 'we do not catch robocopy /MIR' into 'some things are not caught'. (4) NEW IN ROUND 6 — `_claim_probes` holds, for EVERY claim in this block, concrete example strings that the suite really runs through classify(), each with the outcome the sentence promises and a `quote` that must appear verbatim in that sentence. A claim that over-claims coverage goes red, a claim that under-claims goes red, and a claim with no probes at all goes red. That is what makes this a declaration rather than a decoration.",
    "_gate_coverage": {
      "_doc": "One entry per gate id, stating what that gate does NOT see. Mechanically enforced — see _contract. Every concrete string quoted here is also a probe in _claim_probes[\"_gate_coverage.<id>\"], so the sentence and the measurement cannot drift apart.",
      "deploy": "Matches English deploy/ship/publish/roll-out INTENT — the WORDS, not the act. A command line fires only if it happens to contain one of them: `wrangler deploy` fires, while `vercel --prod`, `netlify sites:create`, `gh workflow run release.yml` and `flyctl launch` are all silent even though every one of them deploys. Dutch phrasing — `zet dit live`, the language the owner actually writes in — is silent too. Word-matching cuts the other way as well: `kubectl scale deploy/api --replicas=3` FIRES this gate on the resource short-name, an over-warn measured in round 6.",
      "git-push": "Matches the words `git push origin main` and its relatives. A push performed BY A TOOL is invisible: `gh pr merge 12`, `gh release create v1.0.0`, an MCP git server call, a GUI client, a pre-existing `git config push.default` plus a bare push hidden behind an alias (`git sync`), or any hook/CI step that pushes on your behalf.",
      "spend": "Matches a short list of English money verbs — it sees `charge the customer`, nothing more. It does not see `stripe charges create`, a Mollie/Adyen API call, an `npm publish` that costs a private-registry seat, a paid model/API call inside a script, a cloud resource whose creation bills by the hour, or the same intent in Dutch (`reken de klant af`).",
      "dns-change": "Matches English DNS wording only — `update the nameserver` fires, and that is the whole of it. `aws route53 change-resource-record-sets`, a Cloudflare `/dns_records` API call, a `terraform apply` that happens to contain a zone change, and a registrar's web console are all invisible — and a zone edit is exactly the kind of change that is made through one of those, not by saying 'change the DNS'.",
      "prod-activate": "Matches `activate the production environment` phrasing. A feature-flag flip, `n8n update:workflow --active=true`, a Kubernetes scale-up (`kubectl scale deploy/api --replicas=3`, which does not fire THIS gate — though it does trip the deploy gate by accident on the resource short-name), an environment-variable switch (`NODE_ENV=production`), or a traffic-splitting change that promotes a canary to 100% all move a system into production without ever using those words.",
      "credential-attach": "Requires attach/insert/hardcode plus a credential noun, so `hardcode the token` fires. A literal secret pasted with no verb at all (`9f2c4a7e1b8d3f6a0c5e2d9b4a7f1c3e` is silent — an opaque hex token is deliberately used here instead of a realistic provider-prefixed key, because a real-looking one would trip the doctor's own leak scan), a value written into `.env`/`.npmrc`/a CI secret store, `gh secret set API_KEY`, or a token embedded in a URL are not detected — and the pasted-literal case is the most common one by far.",
      "credential-rotate": "Requires rotate/revoke plus a credential noun, so `rotate the api key` fires. `aws iam delete-access-key`, `gh auth logout`, a provider console 'regenerate' button, deleting an OAuth app, or changing a password through a UI all invalidate a live credential without matching.",
      "workflow-activate": "Requires activate/enable/turn-on plus 'workflow', so `turn on the workflow` fires. The n8n REST call (`PATCH /workflows/42 {active:true}`), the n8n MCP tool, the editor's toggle switch, and importing a workflow whose JSON already has `active:true` are all invisible.",
      "outbound-sms": "Requires one of the listed send/email phrasings with an explicit recipient class, so `send the email to the client` fires. A direct SMTP/Nodemailer/Resend/SendGrid call, a Twilio REST call (`api.twilio.com/2010-04-01/Messages.json`), a Slack/Discord webhook post, `gh issue comment`, an n8n send node, and Dutch phrasing (`stuur de mail naar de klant`) are all outbound actions this gate does not see.",
      "kill-by-name": "Fires on `Stop-Process -Name node` and its documented siblings. See `pid_scoped_kills` (deliberately silent — killing your own PID is what the HARD MUST ALLOWS) and `kill_with_an_invisible_target` (a kill whose victim is only in a variable, or reached through an unlisted Get-Process alias such as `gps`/`ps`). Also invisible: `Stop-Service`, `docker kill`, `systemctl stop`, a Windows Service Manager action, and killing a whole process TREE by PID (`taskkill /T /PID 22420`) which can still take an unrelated child down.",
      "destructive-delete": "The largest surface, and the one five rounds have been spent on. `rm -rf ./src` fires; `robocopy C:\\src C:\\dst /MIR` and `npm run clean` do not, and neither does `rm| -rf ./src`. Not covered: the general case of free-text rewriting (`variable_indirection`, `encoded_or_obfuscated_commands`, `danger_inside_a_script_or_file`, `expansion_only_at_runtime`); destruction by tools that are not deletes at all (`mirroring_and_overwriting_tools`, `truncation_and_raw_device_writes`); POSIX pipeline deletes (`pipeline_deletes_only_partly_covered`); the deliberate and the accidental silences in `non_recursive_deletes` and `regenerable_or_temp_deletes`; and the pattern-level gap in `metachar_outside_the_target`. The valve's own price is in `exact_valve_price`, `alias_and_pipeline_price`, `rm_recurse_arm_price` and `variants_are_not_covered`.",
      "git-destructive": "Fires on `git reset --hard origin/main` and the forms pinned in its examples. See `recoverable_git`. Beyond it: `git worktree remove --force ../wt-a`, `git filter-repo --path secrets --invert-paths`, `git filter-branch` (whose `--tree-filter rm -rf secrets` form does not fire THIS gate — the embedded `rm -rf` trips destructive-delete instead), `git update-ref -d refs/heads/main`, `git reflog expire --expire=now --all` followed by `git gc --prune=now` (which is what actually makes a 'recoverable' reset unrecoverable), `git branch -D feature-x` of an unmerged branch, and a force-push, which is covered by git-push as INTENT text but not as a command line.",
      "write-outside-root": "Fires only when the CALLER supplies BOTH a project_root and a path — given those, `../other-project/secret.js` is flagged. A write expressed purely as shell text (`echo x > ../other/file`, `cp -r . ..`, a heredoc), a tool that never reports a path, a symlink or junction created INSIDE the root that points out of it (the check resolves the target, not links created later), and any write performed by a spawned process are all outside what this check can see."
    },
    "classifier_is_incomplete_by_construction": "THE HONEST HEADLINE. These gates are regular expressions over free shell text. Deciding whether an arbitrary string will destroy something requires knowing which shell will run it, what its variables and aliases hold, what is on disk, and what the programs it invokes will do — none of which a regex has. So there is no version of this file that is complete, and round 6 is not it. Probed both ways: `dd if=/dev/zero of=/dev/sda` is silent while `rm -rf ./src` fires, and the difference between them is not how destructive they are — it is whether the text happened to look like a pattern. Four rounds of the except-valve taught the same lesson from the other side: every time we modelled a bit more shell semantics, the next attack used a bit we had not modelled. What follows is a list of the specific, KNOWN blind spots. It is not exhaustive either, and claiming otherwise would be the exact dishonesty these gates exist to prevent.",
    "variable_indirection": "Anything that moves the dangerous word out of the literal text. Two degrees, both measured, and the difference matters. (a) TARGET hidden: `$t = '.claude'; Remove-Item -Recurse -Force $t` DOES fire — but only on the flags. The classifier never sees what is being deleted, so it cannot tell `.claude` from a scratch directory, and symmetrically a genuinely safe target held in a variable can never be excused. (b) WHOLE COMMAND hidden: `iex $cmd`, `Invoke-Expression $cmd`, `& $tool $args`, `sh -c \"$SCRIPT\"`, `eval \"$CMD\"`, `Start-Process -ArgumentList $a` fire NOTHING — verified silent, because the line carries no delete verb and no flags. Beware the misleading middle case: `$cmd = 'rm -rf .claude'; Invoke-Expression $cmd` does fire, and it is easy to mistake that for detection. It is not — the literal string is simply still sitting in the text (see `prose_over_fire`). Put the assignment on another line, in another file, or behind a concatenation and the warning is gone. This is the cheapest bypass on the list and nothing here defends against it.",
    "encoded_or_obfuscated_commands": "`powershell -EncodedCommand cm0gLXJmIC5jbGF1ZGU=` (that payload decodes to `rm -rf .claude`), `echo cm0gLXJmIC5jbGF1ZGU= | base64 -d | sh`, a hex/ROT-encoded string decoded at runtime, or a command reconstructed from character codes. Both forms are measured silent. The gates see the encoder, not the payload — and the encoder is an ordinary, harmless-looking command. Decoding untrusted payloads to inspect them is not something a zero-dependency advisory classifier should be doing, so this stays open by choice, named rather than defended.",
    "danger_inside_a_script_or_file": "The classifier sees the line it is handed. `npm run clean`, `./scripts/reset.sh`, `pwsh ./tools/wipe.ps1`, `make distclean`, a `postinstall` hook, a Justfile/Taskfile target, or a git hook can each contain any command in this file, and the invocation looks innocent — all four named forms are measured silent. The gates do not read the referenced file, do not resolve package.json scripts, and do not follow a shebang.",
    "mirroring_and_overwriting_tools": "Tools that delete as a SIDE EFFECT of copying are not deletes and are not matched at all — every form below is measured silent: `robocopy C:\\src C:\\dst /MIR` (mirrors, and therefore purges everything in the destination that is not in the source — a routine way to empty a directory by accident), `rsync -a --delete src/ dst/`, `xcopy C:\\src C:\\dst /purge`, `aws s3 sync --delete ./dist s3://bucket`, `gsutil rsync -d ./dist gs://bucket`. None contains a delete verb, none carries a recursive-delete flag, and `/MIR` on a wrong destination is as final as `rm -rf`.",
    "truncation_and_raw_device_writes": "Destroying CONTENT without deleting the file, every one measured silent: `truncate -s 0 important.log`, `: > important.log`, `Clear-Content important.log`, `Set-Content important.log ''`, `dd if=/dev/zero of=/dev/sdX`, `mkfs.ext4 /dev/sdb1`, `diskpart clean`, `Format-Volume -DriveLetter D`, `cipher /w:C`. `dd of=` and `mkfs` in particular are more destructive than anything the delete gate covers, and none of them matches a single arm here.",
    "expansion_only_at_runtime": "CORRECTED IN ROUND 6 — the round-5 text filed these under 'not caught', and that was false in the direction that matters. `rm -rf $EMPTY/` (an unset variable expanding to nothing has famously deleted a root), `rm -rf ./*`, brace expansion `rm -rf {a,b}/../../x` and `find . -name '*.tmp' | xargs rm -rf` all DO fire — measured. They fire on the FLAGS, never on the danger. The real gap is that the classifier reads text while the shell reads a different, later, expanded string, so it can neither recognise a catastrophe nor excuse a safe one: an expanded target can never be byte-identical to one of the 16 literals, so every glob/variable/substitution cleanup over-warns (priced in `exact_valve_price`). What IS silent here is the runtime-expansion form that carries no recursive-plus-force pair at all: `find . -name '*.x' -print0 | xargs -0 rm -f` and `find . -delete`.",
    "pipeline_deletes_only_partly_covered": "match.pattern_line closes the PowerShell form we measured — a recursive `Get-ChildItem`/`gci`/`ls`/`dir` on the LEFT of a pipe feeding a delete verb or `.Delete()` on the right, so `Get-ChildItem .claude -Recurse | Remove-Item -Force` fires. It does NOT cover: the POSIX form `find . -name '*.x' -print0 | xargs -0 rm -f` (the recursion is implicit in `find`, there is no `-r` flag to require) or `find . -delete`; an enumerator that is not one of those four names (`Get-Item .claude -Recurse | Remove-Item -Force`, a variable such as `$items | Remove-Item -Force`, a custom function, `robocopy`); a right-hand delete carrying no recursion flag at all (`Get-ChildItem .claude | Remove-Item -Force`, pinned silent as a no_match); a delete reached via a method on an unlisted object; and any pipeline built across more than one line or assembled from variables. CORRECTED IN ROUND 6: round 5 also listed 'a pipeline whose recursion flag sits on the RIGHT of the pipe with the enumerator unflagged' as uncovered. That was false — `Get-ChildItem .claude | Remove-Item -Recurse -Force` fires, because the right-hand segment is itself a recursive delete and the ordinary segment arm sees it without pattern_line's help.",
    "alias_arm_asymmetry": "CORRECTED IN ROUND 6 — round 5's version of this sentence was untrue, and it promised coverage that did not exist. It said `rm`/`del`/`erase`/`rd`/`rmdir` were 'in both' arms. Read from the live pattern, `rm` was in the POSIX-bundle arm ONLY, so `rm -Recurse .claude` was measured SILENT while `del -Recurse .claude` fired — even though `Get-Alias rm` returns Remove-Item on this machine, exactly like the other four. Round 6 adds a third arm carrying `rm` with the -Recurse abbreviations, and `rm -Recurse .claude` now fires. THE THREE ASYMMETRIES THAT REMAIN, all deliberate, all probed: (1) `rm -r ./emptydir` stays silent while `del -r ./emptydir` warns — the bare `-r` is POSIX's ordinary recursive flag and has been a pinned no_match since round 4, so the two-letter form is genuinely ambiguous between the two shells and only one reading can win; (2) `Remove-Item -Force ./notes.txt` stays silent while `del -Force ./notes.txt` warns, because the bundle arm accepts any single-dash flag containing an 'f' and PowerShell's `-Force` contains both an 'r' and an 'f', so admitting the spelled-out cmdlet there would fire on a single-file delete pinned silent since round 4; (3) `rm --recursive ./src` stays silent — the long POSIX form without a force flag matches no arm, and it was left that way on purpose so the everyday `aws s3 rm s3://bucket --recursive` does not start warning. Named here rather than papered over.",
    "pid_scoped_kills": "`taskkill /PID 22420 /F`, `Stop-Process -Id 22420`, `Stop-Process -InputObject $p`, POSIX `kill 1234`, positional `Stop-Process 22420` (PowerShell binds position 0 to -Id), `Get-Process -Id 22420 | Stop-Process` and `wmic process where processid=22420 delete` never fire — killing one exact PID you own is precisely what the HARD MUST ALLOWS, so flagging it would train the warning away.",
    "kill_with_an_invisible_target": "A kill whose victim is named nowhere in the text does NOT fire: `Stop-Process $procs`, `Stop-Process (Get-Process node)`, `$p | Stop-Process`, or a Get-Process alias (`gps node | Stop-Process`, `ps node | kill`). The classifier only sees the command string, and a bare Stop-Process on its own segment is far more often the PID-scoped form — firing on it produced three measured false alarms, so an explicit -Name / -ProcessName, or a name-scoped `Get-Process … |` on the same line, is required.",
    "regenerable_or_temp_deletes": "EXACTLY 16 command strings are excused (match.except._allowed_segments): a plain recursive delete of node_modules or _scratch via `rm -rf`, `Remove-Item -Recurse -Force`, `npx rimraf` or `rimraf`, with the target written as `node_modules`, `./node_modules`, `_scratch` or `./_scratch` — so `rm -rf node_modules` and `rimraf ./_scratch` are silent. Nothing else. A temp path (`rm -rf /tmp/forge-test-123`, `rm -rf %TEMP%\\forge-x`), a sub-path (`rm -rf ./_scratch/run-1`), a Windows-style path (`Remove-Item -Recurse -Force .\\node_modules`), a prefix (`sudo rm -rf node_modules`, `npx --yes rimraf node_modules`), a trailing slash, a different spelling of the same cmdlet (`rm -Recurse node_modules`) or different spacing all WARN — they are not in the list, and the valve does not interpret, only compare.",
    "exact_valve_price": "EXACT EQUALITY, NO PARSER (2026-08-01, round 4, owner directive). The measured cost on a 44-command corpus of genuinely legitimate cleanup: 30 warn, up from 10 — i.e. 20 NEW over-warnings, and only 14 of 44 real cleanup commands stay silent (`rm -rf node_modules` is one of the 14). The new warnings are, by cause: PREFIXED forms (`sudo rm -rf node_modules`, `npx --yes rimraf node_modules`, `pnpm dlx rimraf node_modules`); SUB-PATHS of a safe dir (`rm -rf ./_scratch/run-1`, `rm -rf ./_scratch/run-1 && npm ci`, `rm -rf ./_scratch/run-1;`, `rm -rf ./_scratch/run-1 || true`, `rm -rf ./_scratch/tmp-artifacts`); TEMP paths of every flavour (`rm -rf /tmp/forge-test-123`, `rm -rf /tmp/forge-test-123 && echo done`, `rm -rf /var/tmp/build-cache`, `sudo rm -rf /tmp/stale-lock`, `rm -rf ./temp`, `rm -rf ./temp/build`, `rm -rf ./tmp/cache`, `rm -rf C:/Users/.../Temp/claude/scratchpad/x`, `Remove-Item -Recurse \"C:\\Temp\\a b\"`, `Remove-Item -Recurse -Force \"$env:TEMP\\forge-x\"`); WINDOWS-STYLE paths (`Remove-Item -Recurse -Force .\\node_modules`, `Remove-Item -Recurse -Force .\\_scratch\\run-1`); and the round-3 price that remains (env vars, globs, `~`, command substitution, a glued `;`). This is deliberate. The gate is ADVISORY — it prints a warning, it never blocks — so noise costs a moment's reading, while a bypass cost this project four rounds. Widening the list is a config edit of literal strings, not a code change, and it can never introduce a parsing bug.",
    "alias_and_pipeline_price": "ROUND 5's OWN PRICE, measured 2026-08-01 by running one corpus through the round-4 pattern and the round-5 pattern in the same process. The round-4 44-command corpus did not move at all: 30 of 30 priced warnings still warn, 14 of 14 priced silences are still silent — the alias arms and pattern_line cost nothing there. The new noise is on a separate 34-command corpus of legitimate alias/pipeline cleanup: 3 warned already (the `/s` DOS forms), 24 warn now, so 21 are NEW over-warnings, and 10 stay silent (`del notes.txt` and `rd emptydir` among them). By cause: ALIAS FORMS OF THE EXCUSED LITERALS (`del -Recurse -Force node_modules`, `rd -Recurse -Force ./_scratch`, `rmdir -Recurse -Force node_modules`, `del -r -force node_modules`, `rd -rf node_modules`, `del -Recurse -Force .\\node_modules`) — the 16-literal valve knows `rm`, `Remove-Item`, `npx rimraf` and `rimraf` only, and it compares, so no alias spelling can ever be excused; SINGLE-FILE ALIAS DELETES carrying `-Force` (`del -Force ./notes.txt`, `rd -Force ./tmpfile`, `erase -Force ./old.log`) — the inherited `-Force`-contains-r-and-f quirk described in `alias_arm_asymmetry`; EMPTY-DIRECTORY RECURSIVE DELETES (`del -r ./emptydir`, `rd -r ./emptydir`) — consistent with `Remove-Item -r` firing since round 4; and PIPELINE CLEANUPS (`Get-ChildItem ./logs -Recurse -Filter *.log | Remove-Item -Force`, `Get-ChildItem ./_scratch -Recurse | Remove-Item -Force`, `gci node_modules -r | ri -force`, `Get-ChildItem . -Recurse -Include *.tmp | Remove-Item`, `Get-ChildItem ./dist -Recurse | Remove-Item -Force -ErrorAction SilentlyContinue`, `dir ./_scratch -Recurse | del`) — pattern_line has no except valve by design, exactly like kill-by-name's. Combined across both corpora: 54 of 78 legitimate cleanup commands warn. The gate is ADVISORY; it prints and never blocks. Widening the valve is a config edit of literal strings, never a code change.",
    "rm_recurse_arm_price": "ROUND 6's OWN PRICE, measured the way rounds 4 and 5 measured theirs: one corpus through the round-5 pattern and the shipped pattern in the same process, with arm 3 spliced out to make the counterfactual. It is small because the new arm is narrow — arm 3 fires only on `rm` plus a -Recurse abbreviation of at least two letters, so the only commands that can change verdict are the PowerShell-style recursive deletes nobody could spell before. On a 12-command corpus of legitimate `rm`-shaped cleanup: 5 warn, 1 of which already warned in round 5 (`rm -Recurse -Force node_modules`, which arm 1 caught because `-Force` carries both an 'r' and an 'f'), so 4 are NEW — `rm -Recurse node_modules`, `rm -Recurse ./node_modules`, `rm -Recurse ./_scratch`, `rm -rec node_modules`. Every one of the four is an alias spelling the 16-literal valve does not know, which is exactly the cause already priced in `alias_and_pipeline_price`. Seven stay silent and nothing that was quiet for a different reason became noisy: `rm -r ./emptydir`, `rm -rf node_modules`, `rm --force ./notes.txt`, `docker rm -f mycontainer`, `aws s3 rm s3://bucket --recursive`, `rm --recursive ./src` and `rm -r ./src`. The round-4 and round-5 priced silences were re-run and 0 of 24 moved. Total across all three corpora: 59 of 90 legitimate cleanup commands warn.",
    "variants_are_not_covered": "Because the valve compares and never interprets, EVERY variant of an allowed command warns: different case (`RM -RF node_modules`), extra spaces (`rm  -rf  node_modules`), a different flag order (`rm -fr node_modules`), quotes (`rm -rf \"node_modules\"`), a trailing slash (`rm -rf node_modules/`). That is the intended shape of the trade, not an oversight.",
    "metachar_outside_the_target": "The invariant sweep covers a metacharacter anywhere in the delete command. A metacharacter injected into the COMMAND WORD or its FLAGS can still mute a gate: `rm| -rf ./src` splits into `rm` (no flags) and `-rf ./src` (no command) and fires nothing, while the ordinary `rm -rf ./src` fires. That muted string is not a runnable delete — `rm` with no target deletes nothing and `-rf ./src` is not a command — so it is a detection gap in the PATTERN, not a hole in the valve. Named here so it is a known gap rather than a silent one.",
    "build_output_deletes": "`rm -rf dist`, `rm -rf build`, `rm -rf .next` and friends DO fire. They are regenerable in principle, but the classifier cannot tell a build directory from a source directory by name, so they are NOT excused. The excuse list holds exactly 16 literal strings — 4 command prefixes x 4 argument tails — covering only two directories, `node_modules` and `_scratch`; see `regenerable_or_temp_deletes` for the full list. (Corrected 2026-08-01: this sentence previously claimed the list was \"the three unambiguous cases above\", which was never true and contradicted `regenerable_or_temp_deletes` in this same file. Found by the sixth witness; outcome-probes cannot catch a wrong justification, only a wrong outcome.)",
    "non_recursive_deletes": "THE KEY NAME IS A LEGACY MISNOMER AND ROUND 5's JUSTIFICATION WAS FACTUALLY WRONG — corrected in round 6. The old sentence read '… do not fire — they are single/interactive deletes, not tree destruction.' Two of the commands it covered are not single deletes at all: `rm -r ./src` and `rm --recursive ./src` are ORDINARY POSIX TREE DELETES (`rm -r` removes a directory and everything under it; `-f` only suppresses prompts), and both are measured silent. So this entry is really two different things. (1) TRUE SINGLE-FILE / EMPTY-DIRECTORY SILENCES, deliberate, because they destroy exactly one thing: `rm ./notes.txt`, `rm -f ./notes.txt`, `rm --force ./notes.txt`, `Remove-Item ./notes.txt`, `del notes.txt`, `rmdir emptydir`. (2) REAL TREE DELETES THAT STAY SILENT — a gap, not a design: `rm -r ./src` and `rm --recursive ./src`. The bundle arm demands a force flag as well; round 6's `rm`-recurse arm starts at `-re`, because claiming the bare `-r` would break the `rm -r ./emptydir` no_match pin (see `alias_arm_asymmetry`); and `--recursive` was left out so that `aws s3 rm s3://bucket --recursive` does not start warning. Write the same delete as `del -r ./src` and it warns.",
    "recoverable_git": "`git branch -D feature-x`, `git revert HEAD`, `git reset --soft HEAD~1` and `git stash pop` (any stash other than drop/clear) do not fire — the work is still reachable. `git worktree remove --force ../wt-a` is also NOT covered by this pass, even though this project uses worktrees: it deletes one worktree directory, and the branch/commits behind it survive.",
    "out_of_scope_this_pass": "NOT covered by this pass, all measured silent: SQL/DDL destruction (`DROP TABLE users`, `TRUNCATE TABLE leads`), container/cluster deletes (`docker system prune -af`, `kubectl delete namespace prod`), cloud CLIs (`aws s3 rm s3://bucket --recursive`, `gcloud compute instances delete api-1`), package publishing (`npm publish`), disk/host commands (mkfs, dd, `shutdown /r /t 0`), Windows service/registry destruction (`sc delete myservice`, `reg delete HKLM\\Software\\X /f`) and CI/secret-store deletions. Named here so their absence is a known gap, not a silent one.",
    "prose_over_fire": "The gates are matched against whatever text a caller passes, so PROSE that merely quotes a destructive command fires too: `never run rm -rf on src` and `do not use git reset --hard here` both warn. The same accident happens inside ordinary command text — `kubectl scale deploy/api --replicas=3` fires the DEPLOY gate on the resource short-name, and `git filter-branch --tree-filter rm -rf secrets` fires destructive-delete on the embedded `rm -rf` rather than on the history rewrite (both measured in round 6). Accepted on purpose: an advisory over-warn is cheap, a missed real command is what cost the gateway on 2026-07-29.",
    "no_shell_parser": "splitCommands() honours no quoting/escaping, so a separator inside a quoted argument still splits — `rm -rf \"./_scratch/a|b/../../.claude\"` fires. That direction only ever creates MORE segments to test (fail-safe for detection), and it cannot weaken the valve: splitCommandsDetailed() marks a cut segment `intact:false`, so `rm -rf node_modules$(echo /../.claude)` fires on its stump instead of being excused, while a merely trailing separator is not amputation and `rm -rf node_modules;` stays silent. match.pattern_line is deliberately NOT segment-split and has no except valve — it exists precisely to see across a pipeline, which is why `Get-ChildItem .claude -Recurse | Remove-Item -Force` fires.",
    "_claim_probes": {
      "_doc": "(c) THE VERIFIABLE HALF OF THIS DECLARATION (round 6, owner directive). Every claim above is paired here with concrete example strings that forge-actiongate.test.cjs section 2f really puts through classify(). Three things are asserted per probe: the string behaves as declared (`expect`), the string is quoted verbatim in the claim's own prose (`quote`), and the claim/probe key sets are a bijection. A sentence saying 'X is not caught' therefore goes RED the day X starts firing, and a sentence saying 'X fires' goes RED the day it stops. WHAT THIS DOES NOT CATCH, stated so this block does not repeat the sin it fixes: a probe binds an OUTCOME, not a JUSTIFICATION. Round 5's `non_recursive_deletes` had every outcome right and the reason wrong ('single/interactive deletes' about a command that deletes a whole tree) — no probe would have gone red. Reasons still need a reader.",
      "_form": "{ text, expect, gates?, quote, path?, project_root? }. expect:'silent' = classify().matched must be exactly [] (nothing at all fires); expect:'fires' = matched must include every id in `gates`; expect:'not' = matched must include none of them (another gate may legitimately fire). `quote` must be >= 4 chars and appear BOTH in the probe's own text/path AND in the prose of the claim it hangs under — that is the weld between the sentence and the measurement. `path`/`project_root` are for the isolation gate, which cannot be probed with text alone.",
      "_gate_coverage.deploy": [
        {
          "text": "wrangler deploy",
          "expect": "fires",
          "gates": [
            "deploy"
          ],
          "quote": "wrangler deploy"
        },
        {
          "text": "vercel --prod",
          "expect": "silent",
          "quote": "vercel --prod"
        },
        {
          "text": "netlify sites:create",
          "expect": "silent",
          "quote": "netlify sites:create"
        },
        {
          "text": "gh workflow run release.yml",
          "expect": "silent",
          "quote": "gh workflow run release.yml"
        },
        {
          "text": "flyctl launch",
          "expect": "silent",
          "quote": "flyctl launch"
        },
        {
          "text": "zet dit live",
          "expect": "silent",
          "quote": "zet dit live"
        },
        {
          "text": "kubectl scale deploy/api --replicas=3",
          "expect": "fires",
          "gates": [
            "deploy"
          ],
          "quote": "kubectl scale deploy/api --replicas=3"
        }
      ],
      "_gate_coverage.git-push": [
        {
          "text": "git push origin main",
          "expect": "fires",
          "gates": [
            "git-push"
          ],
          "quote": "git push origin main"
        },
        {
          "text": "gh pr merge 12",
          "expect": "silent",
          "quote": "gh pr merge"
        },
        {
          "text": "gh release create v1.0.0",
          "expect": "silent",
          "quote": "gh release create"
        },
        {
          "text": "git sync",
          "expect": "silent",
          "quote": "git sync"
        }
      ],
      "_gate_coverage.spend": [
        {
          "text": "charge the customer for this order",
          "expect": "fires",
          "gates": [
            "spend"
          ],
          "quote": "charge the customer"
        },
        {
          "text": "stripe charges create",
          "expect": "silent",
          "quote": "stripe charges create"
        },
        {
          "text": "npm publish",
          "expect": "silent",
          "quote": "npm publish"
        },
        {
          "text": "reken de klant af",
          "expect": "silent",
          "quote": "reken de klant af"
        }
      ],
      "_gate_coverage.dns-change": [
        {
          "text": "update the nameserver",
          "expect": "fires",
          "gates": [
            "dns-change"
          ],
          "quote": "update the nameserver"
        },
        {
          "text": "aws route53 change-resource-record-sets --hosted-zone-id Z1",
          "expect": "silent",
          "quote": "aws route53 change-resource-record-sets"
        },
        {
          "text": "terraform apply",
          "expect": "silent",
          "quote": "terraform apply"
        }
      ],
      "_gate_coverage.prod-activate": [
        {
          "text": "activate the production environment now",
          "expect": "fires",
          "gates": [
            "prod-activate"
          ],
          "quote": "activate the production environment"
        },
        {
          "text": "n8n update:workflow --active=true",
          "expect": "silent",
          "quote": "n8n update:workflow --active=true"
        },
        {
          "text": "NODE_ENV=production npm start",
          "expect": "silent",
          "quote": "NODE_ENV=production"
        },
        {
          "text": "kubectl scale deploy/api --replicas=3",
          "expect": "not",
          "gates": [
            "prod-activate"
          ],
          "quote": "kubectl scale deploy/api --replicas=3"
        }
      ],
      "_gate_coverage.credential-attach": [
        {
          "text": "hardcode the token",
          "expect": "fires",
          "gates": [
            "credential-attach"
          ],
          "quote": "hardcode the token"
        },
        {
          "text": "gh secret set API_KEY",
          "expect": "silent",
          "quote": "gh secret set API_KEY"
        },
        {
          "text": "9f2c4a7e1b8d3f6a0c5e2d9b4a7f1c3e",
          "expect": "silent",
          "quote": "9f2c4a7e1b8d3f6a0c5e2d9b4a7f1c3e"
        }
      ],
      "_gate_coverage.credential-rotate": [
        {
          "text": "rotate the api key immediately",
          "expect": "fires",
          "gates": [
            "credential-rotate"
          ],
          "quote": "rotate the api key"
        },
        {
          "text": "aws iam delete-access-key --access-key-id AKIAEXAMPLE",
          "expect": "silent",
          "quote": "aws iam delete-access-key"
        },
        {
          "text": "gh auth logout",
          "expect": "silent",
          "quote": "gh auth logout"
        }
      ],
      "_gate_coverage.workflow-activate": [
        {
          "text": "turn on the workflow",
          "expect": "fires",
          "gates": [
            "workflow-activate"
          ],
          "quote": "turn on the workflow"
        },
        {
          "text": "PATCH /workflows/42 {active:true}",
          "expect": "silent",
          "quote": "PATCH /workflows/42 {active:true}"
        }
      ],
      "_gate_coverage.outbound-sms": [
        {
          "text": "send the email to the client",
          "expect": "fires",
          "gates": [
            "outbound-sms"
          ],
          "quote": "send the email to the client"
        },
        {
          "text": "stuur de mail naar de klant",
          "expect": "silent",
          "quote": "stuur de mail naar de klant"
        },
        {
          "text": "curl -X POST https://api.twilio.com/2010-04-01/Messages.json",
          "expect": "silent",
          "quote": "api.twilio.com/2010-04-01/Messages.json"
        },
        {
          "text": "gh issue comment 12 --body hi",
          "expect": "silent",
          "quote": "gh issue comment"
        }
      ],
      "_gate_coverage.kill-by-name": [
        {
          "text": "Stop-Process -Name node",
          "expect": "fires",
          "gates": [
            "kill-by-name"
          ],
          "quote": "Stop-Process -Name node"
        },
        {
          "text": "Stop-Service w3svc",
          "expect": "silent",
          "quote": "Stop-Service"
        },
        {
          "text": "docker kill mycontainer",
          "expect": "silent",
          "quote": "docker kill"
        },
        {
          "text": "systemctl stop nginx",
          "expect": "silent",
          "quote": "systemctl stop"
        },
        {
          "text": "taskkill /T /PID 22420",
          "expect": "silent",
          "quote": "taskkill /T /PID 22420"
        }
      ],
      "_gate_coverage.destructive-delete": [
        {
          "text": "rm -rf ./src",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf ./src"
        },
        {
          "text": "robocopy C:\\src C:\\dst /MIR",
          "expect": "silent",
          "quote": "robocopy C:\\src C:\\dst /MIR"
        },
        {
          "text": "npm run clean",
          "expect": "silent",
          "quote": "npm run clean"
        },
        {
          "text": "rm| -rf ./src",
          "expect": "silent",
          "quote": "rm| -rf ./src"
        }
      ],
      "_gate_coverage.git-destructive": [
        {
          "text": "git reset --hard origin/main",
          "expect": "fires",
          "gates": [
            "git-destructive"
          ],
          "quote": "git reset --hard origin/main"
        },
        {
          "text": "git worktree remove --force ../wt-a",
          "expect": "silent",
          "quote": "git worktree remove --force ../wt-a"
        },
        {
          "text": "git filter-repo --path secrets --invert-paths",
          "expect": "silent",
          "quote": "git filter-repo --path secrets --invert-paths"
        },
        {
          "text": "git filter-branch --tree-filter rm -rf secrets",
          "expect": "not",
          "gates": [
            "git-destructive"
          ],
          "quote": "git filter-branch"
        },
        {
          "text": "git update-ref -d refs/heads/main",
          "expect": "silent",
          "quote": "git update-ref -d refs/heads/main"
        },
        {
          "text": "git reflog expire --expire=now --all",
          "expect": "silent",
          "quote": "git reflog expire --expire=now --all"
        },
        {
          "text": "git gc --prune=now",
          "expect": "silent",
          "quote": "git gc --prune=now"
        },
        {
          "text": "git branch -D feature-x",
          "expect": "silent",
          "quote": "git branch -D feature-x"
        }
      ],
      "_gate_coverage.write-outside-root": [
        {
          "text": "writing a generated file",
          "expect": "fires",
          "gates": [
            "write-outside-root"
          ],
          "quote": "../other-project/secret.js",
          "path": "../other-project/secret.js",
          "project_root": "."
        },
        {
          "text": "echo x > ../other/file",
          "expect": "silent",
          "quote": "echo x > ../other/file"
        },
        {
          "text": "cp -r . ..",
          "expect": "silent",
          "quote": "cp -r . .."
        }
      ],
      "classifier_is_incomplete_by_construction": [
        {
          "text": "dd if=/dev/zero of=/dev/sda",
          "expect": "silent",
          "quote": "dd if=/dev/zero of=/dev/sda"
        },
        {
          "text": "rm -rf ./src",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf ./src"
        }
      ],
      "variable_indirection": [
        {
          "text": "$t = '.claude'; Remove-Item -Recurse -Force $t",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "Remove-Item -Recurse -Force $t"
        },
        {
          "text": "iex $cmd",
          "expect": "silent",
          "quote": "iex $cmd"
        },
        {
          "text": "Invoke-Expression $cmd",
          "expect": "silent",
          "quote": "Invoke-Expression $cmd"
        },
        {
          "text": "& $tool $args",
          "expect": "silent",
          "quote": "& $tool $args"
        },
        {
          "text": "sh -c \"$SCRIPT\"",
          "expect": "silent",
          "quote": "sh -c \"$SCRIPT\""
        },
        {
          "text": "eval \"$CMD\"",
          "expect": "silent",
          "quote": "eval \"$CMD\""
        },
        {
          "text": "Start-Process -ArgumentList $a",
          "expect": "silent",
          "quote": "Start-Process -ArgumentList $a"
        },
        {
          "text": "$cmd = 'rm -rf .claude'; Invoke-Expression $cmd",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "$cmd = 'rm -rf .claude'"
        }
      ],
      "encoded_or_obfuscated_commands": [
        {
          "text": "powershell -EncodedCommand cm0gLXJmIC5jbGF1ZGU=",
          "expect": "silent",
          "quote": "powershell -EncodedCommand cm0gLXJmIC5jbGF1ZGU="
        },
        {
          "text": "echo cm0gLXJmIC5jbGF1ZGU= | base64 -d | sh",
          "expect": "silent",
          "quote": "base64 -d | sh"
        }
      ],
      "danger_inside_a_script_or_file": [
        {
          "text": "npm run clean",
          "expect": "silent",
          "quote": "npm run clean"
        },
        {
          "text": "./scripts/reset.sh",
          "expect": "silent",
          "quote": "./scripts/reset.sh"
        },
        {
          "text": "pwsh ./tools/wipe.ps1",
          "expect": "silent",
          "quote": "pwsh ./tools/wipe.ps1"
        },
        {
          "text": "make distclean",
          "expect": "silent",
          "quote": "make distclean"
        }
      ],
      "mirroring_and_overwriting_tools": [
        {
          "text": "robocopy C:\\src C:\\dst /MIR",
          "expect": "silent",
          "quote": "robocopy C:\\src C:\\dst /MIR"
        },
        {
          "text": "rsync -a --delete src/ dst/",
          "expect": "silent",
          "quote": "rsync -a --delete src/ dst/"
        },
        {
          "text": "xcopy C:\\src C:\\dst /purge",
          "expect": "silent",
          "quote": "xcopy C:\\src C:\\dst /purge"
        },
        {
          "text": "aws s3 sync --delete ./dist s3://bucket",
          "expect": "silent",
          "quote": "aws s3 sync --delete ./dist s3://bucket"
        },
        {
          "text": "gsutil rsync -d ./dist gs://bucket",
          "expect": "silent",
          "quote": "gsutil rsync -d ./dist gs://bucket"
        }
      ],
      "truncation_and_raw_device_writes": [
        {
          "text": "truncate -s 0 important.log",
          "expect": "silent",
          "quote": "truncate -s 0 important.log"
        },
        {
          "text": ": > important.log",
          "expect": "silent",
          "quote": ": > important.log"
        },
        {
          "text": "Clear-Content important.log",
          "expect": "silent",
          "quote": "Clear-Content important.log"
        },
        {
          "text": "Set-Content important.log ''",
          "expect": "silent",
          "quote": "Set-Content important.log ''"
        },
        {
          "text": "dd if=/dev/zero of=/dev/sdX",
          "expect": "silent",
          "quote": "dd if=/dev/zero of=/dev/sdX"
        },
        {
          "text": "mkfs.ext4 /dev/sdb1",
          "expect": "silent",
          "quote": "mkfs.ext4 /dev/sdb1"
        },
        {
          "text": "diskpart clean",
          "expect": "silent",
          "quote": "diskpart clean"
        },
        {
          "text": "Format-Volume -DriveLetter D",
          "expect": "silent",
          "quote": "Format-Volume -DriveLetter D"
        },
        {
          "text": "cipher /w:C",
          "expect": "silent",
          "quote": "cipher /w:C"
        }
      ],
      "expansion_only_at_runtime": [
        {
          "text": "rm -rf $EMPTY/",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf $EMPTY/"
        },
        {
          "text": "rm -rf ./*",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf ./*"
        },
        {
          "text": "rm -rf {a,b}/../../x",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf {a,b}/../../x"
        },
        {
          "text": "find . -name '*.tmp' | xargs rm -rf",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "find . -name '*.tmp' | xargs rm -rf"
        },
        {
          "text": "find . -name '*.x' -print0 | xargs -0 rm -f",
          "expect": "silent",
          "quote": "find . -name '*.x' -print0 | xargs -0 rm -f"
        },
        {
          "text": "find . -delete",
          "expect": "silent",
          "quote": "find . -delete"
        }
      ],
      "pipeline_deletes_only_partly_covered": [
        {
          "text": "Get-ChildItem .claude -Recurse | Remove-Item -Force",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "Get-ChildItem .claude -Recurse | Remove-Item -Force"
        },
        {
          "text": "Get-ChildItem .claude | Remove-Item -Recurse -Force",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "Get-ChildItem .claude | Remove-Item -Recurse -Force"
        },
        {
          "text": "find . -name '*.x' -print0 | xargs -0 rm -f",
          "expect": "silent",
          "quote": "find . -name '*.x' -print0 | xargs -0 rm -f"
        },
        {
          "text": "find . -delete",
          "expect": "silent",
          "quote": "find . -delete"
        },
        {
          "text": "Get-Item .claude -Recurse | Remove-Item -Force",
          "expect": "silent",
          "quote": "Get-Item .claude -Recurse | Remove-Item -Force"
        },
        {
          "text": "$items | Remove-Item -Force",
          "expect": "silent",
          "quote": "$items | Remove-Item -Force"
        },
        {
          "text": "Get-ChildItem .claude | Remove-Item -Force",
          "expect": "silent",
          "quote": "Get-ChildItem .claude | Remove-Item -Force"
        }
      ],
      "alias_arm_asymmetry": [
        {
          "text": "rm -Recurse .claude",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -Recurse .claude"
        },
        {
          "text": "del -Recurse .claude",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "del -Recurse .claude"
        },
        {
          "text": "del -r ./emptydir",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "del -r ./emptydir"
        },
        {
          "text": "del -Force ./notes.txt",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "del -Force ./notes.txt"
        },
        {
          "text": "rm -r ./emptydir",
          "expect": "silent",
          "quote": "rm -r ./emptydir"
        },
        {
          "text": "Remove-Item -Force ./notes.txt",
          "expect": "silent",
          "quote": "Remove-Item -Force ./notes.txt"
        },
        {
          "text": "rm --recursive ./src",
          "expect": "silent",
          "quote": "rm --recursive ./src"
        },
        {
          "text": "aws s3 rm s3://bucket --recursive",
          "expect": "silent",
          "quote": "aws s3 rm s3://bucket --recursive"
        }
      ],
      "pid_scoped_kills": [
        {
          "text": "taskkill /PID 22420 /F",
          "expect": "silent",
          "quote": "taskkill /PID 22420 /F"
        },
        {
          "text": "Stop-Process -Id 22420",
          "expect": "silent",
          "quote": "Stop-Process -Id 22420"
        },
        {
          "text": "Stop-Process -InputObject $p",
          "expect": "silent",
          "quote": "Stop-Process -InputObject $p"
        },
        {
          "text": "kill 1234",
          "expect": "silent",
          "quote": "kill 1234"
        },
        {
          "text": "Stop-Process 22420",
          "expect": "silent",
          "quote": "Stop-Process 22420"
        },
        {
          "text": "Get-Process -Id 22420 | Stop-Process",
          "expect": "silent",
          "quote": "Get-Process -Id 22420 | Stop-Process"
        },
        {
          "text": "wmic process where processid=22420 delete",
          "expect": "silent",
          "quote": "wmic process where processid=22420 delete"
        }
      ],
      "kill_with_an_invisible_target": [
        {
          "text": "Stop-Process $procs",
          "expect": "silent",
          "quote": "Stop-Process $procs"
        },
        {
          "text": "Stop-Process (Get-Process node)",
          "expect": "silent",
          "quote": "Stop-Process (Get-Process node)"
        },
        {
          "text": "$p | Stop-Process",
          "expect": "silent",
          "quote": "$p | Stop-Process"
        },
        {
          "text": "gps node | Stop-Process",
          "expect": "silent",
          "quote": "gps node | Stop-Process"
        },
        {
          "text": "ps node | kill",
          "expect": "silent",
          "quote": "ps node | kill"
        }
      ],
      "regenerable_or_temp_deletes": [
        {
          "text": "rm -rf node_modules",
          "expect": "silent",
          "quote": "rm -rf node_modules"
        },
        {
          "text": "rimraf ./_scratch",
          "expect": "silent",
          "quote": "./_scratch"
        },
        {
          "text": "rm -rf /tmp/forge-test-123",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf /tmp/forge-test-123"
        },
        {
          "text": "rm -rf %TEMP%\\forge-x",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf %TEMP%\\forge-x"
        },
        {
          "text": "rm -rf ./_scratch/run-1",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf ./_scratch/run-1"
        },
        {
          "text": "Remove-Item -Recurse -Force .\\node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "Remove-Item -Recurse -Force .\\node_modules"
        },
        {
          "text": "sudo rm -rf node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "sudo rm -rf node_modules"
        },
        {
          "text": "npx --yes rimraf node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "npx --yes rimraf node_modules"
        },
        {
          "text": "rm -Recurse node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -Recurse node_modules"
        }
      ],
      "exact_valve_price": [
        {
          "text": "sudo rm -rf node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "sudo rm -rf node_modules"
        },
        {
          "text": "pnpm dlx rimraf node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "pnpm dlx rimraf node_modules"
        },
        {
          "text": "rm -rf ./_scratch/run-1",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf ./_scratch/run-1"
        },
        {
          "text": "rm -rf /var/tmp/build-cache",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf /var/tmp/build-cache"
        },
        {
          "text": "Remove-Item -Recurse -Force .\\_scratch\\run-1",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "Remove-Item -Recurse -Force .\\_scratch\\run-1"
        },
        {
          "text": "rm -rf node_modules",
          "expect": "silent",
          "quote": "rm -rf node_modules"
        }
      ],
      "alias_and_pipeline_price": [
        {
          "text": "del -Recurse -Force node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "del -Recurse -Force node_modules"
        },
        {
          "text": "rd -rf node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rd -rf node_modules"
        },
        {
          "text": "del -Force ./notes.txt",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "del -Force ./notes.txt"
        },
        {
          "text": "del -r ./emptydir",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "del -r ./emptydir"
        },
        {
          "text": "Get-ChildItem ./_scratch -Recurse | Remove-Item -Force",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "Get-ChildItem ./_scratch -Recurse | Remove-Item -Force"
        },
        {
          "text": "del notes.txt",
          "expect": "silent",
          "quote": "del notes.txt"
        },
        {
          "text": "rd emptydir",
          "expect": "silent",
          "quote": "rd emptydir"
        }
      ],
      "rm_recurse_arm_price": [
        {
          "text": "rm -Recurse node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -Recurse node_modules"
        },
        {
          "text": "rm -Recurse ./node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -Recurse ./node_modules"
        },
        {
          "text": "rm -Recurse ./_scratch",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -Recurse ./_scratch"
        },
        {
          "text": "rm -rec node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rec node_modules"
        },
        {
          "text": "rm -Recurse -Force node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -Recurse -Force node_modules"
        },
        {
          "text": "rm -r ./emptydir",
          "expect": "silent",
          "quote": "rm -r ./emptydir"
        },
        {
          "text": "rm -rf node_modules",
          "expect": "silent",
          "quote": "rm -rf node_modules"
        },
        {
          "text": "rm --force ./notes.txt",
          "expect": "silent",
          "quote": "rm --force ./notes.txt"
        },
        {
          "text": "docker rm -f mycontainer",
          "expect": "silent",
          "quote": "docker rm -f mycontainer"
        },
        {
          "text": "aws s3 rm s3://bucket --recursive",
          "expect": "silent",
          "quote": "aws s3 rm s3://bucket --recursive"
        },
        {
          "text": "rm --recursive ./src",
          "expect": "silent",
          "quote": "rm --recursive ./src"
        },
        {
          "text": "rm -r ./src",
          "expect": "silent",
          "quote": "rm -r ./src"
        }
      ],
      "variants_are_not_covered": [
        {
          "text": "RM -RF node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "RM -RF node_modules"
        },
        {
          "text": "rm  -rf  node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm  -rf  node_modules"
        },
        {
          "text": "rm -fr node_modules",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -fr node_modules"
        },
        {
          "text": "rm -rf \"node_modules\"",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf \"node_modules\""
        },
        {
          "text": "rm -rf node_modules/",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf node_modules/"
        }
      ],
      "metachar_outside_the_target": [
        {
          "text": "rm| -rf ./src",
          "expect": "silent",
          "quote": "rm| -rf ./src"
        },
        {
          "text": "rm -rf ./src",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf ./src"
        }
      ],
      "build_output_deletes": [
        {
          "text": "rm -rf dist",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf dist"
        },
        {
          "text": "rm -rf build",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf build"
        },
        {
          "text": "rm -rf .next",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf .next"
        }
      ],
      "non_recursive_deletes": [
        {
          "text": "rm ./notes.txt",
          "expect": "silent",
          "quote": "rm ./notes.txt"
        },
        {
          "text": "rm -f ./notes.txt",
          "expect": "silent",
          "quote": "rm -f ./notes.txt"
        },
        {
          "text": "rm --force ./notes.txt",
          "expect": "silent",
          "quote": "rm --force ./notes.txt"
        },
        {
          "text": "Remove-Item ./notes.txt",
          "expect": "silent",
          "quote": "Remove-Item ./notes.txt"
        },
        {
          "text": "del notes.txt",
          "expect": "silent",
          "quote": "del notes.txt"
        },
        {
          "text": "rmdir emptydir",
          "expect": "silent",
          "quote": "rmdir emptydir"
        },
        {
          "text": "rm -r ./src",
          "expect": "silent",
          "quote": "rm -r ./src"
        },
        {
          "text": "rm --recursive ./src",
          "expect": "silent",
          "quote": "rm --recursive ./src"
        },
        {
          "text": "del -r ./src",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "del -r ./src"
        }
      ],
      "recoverable_git": [
        {
          "text": "git branch -D feature-x",
          "expect": "silent",
          "quote": "git branch -D feature-x"
        },
        {
          "text": "git revert HEAD",
          "expect": "silent",
          "quote": "git revert HEAD"
        },
        {
          "text": "git reset --soft HEAD~1",
          "expect": "silent",
          "quote": "git reset --soft HEAD~1"
        },
        {
          "text": "git stash pop",
          "expect": "silent",
          "quote": "git stash pop"
        },
        {
          "text": "git worktree remove --force ../wt-a",
          "expect": "silent",
          "quote": "git worktree remove --force ../wt-a"
        }
      ],
      "out_of_scope_this_pass": [
        {
          "text": "DROP TABLE users",
          "expect": "silent",
          "quote": "DROP TABLE users"
        },
        {
          "text": "TRUNCATE TABLE leads",
          "expect": "silent",
          "quote": "TRUNCATE TABLE leads"
        },
        {
          "text": "docker system prune -af",
          "expect": "silent",
          "quote": "docker system prune -af"
        },
        {
          "text": "kubectl delete namespace prod",
          "expect": "silent",
          "quote": "kubectl delete namespace prod"
        },
        {
          "text": "aws s3 rm s3://bucket --recursive",
          "expect": "silent",
          "quote": "aws s3 rm s3://bucket --recursive"
        },
        {
          "text": "gcloud compute instances delete api-1",
          "expect": "silent",
          "quote": "gcloud compute instances delete api-1"
        },
        {
          "text": "npm publish",
          "expect": "silent",
          "quote": "npm publish"
        },
        {
          "text": "shutdown /r /t 0",
          "expect": "silent",
          "quote": "shutdown /r /t 0"
        },
        {
          "text": "sc delete myservice",
          "expect": "silent",
          "quote": "sc delete myservice"
        },
        {
          "text": "reg delete HKLM\\Software\\X /f",
          "expect": "silent",
          "quote": "reg delete HKLM\\Software\\X /f"
        }
      ],
      "prose_over_fire": [
        {
          "text": "never run rm -rf on src",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "never run rm -rf on src"
        },
        {
          "text": "do not use git reset --hard here",
          "expect": "fires",
          "gates": [
            "git-destructive"
          ],
          "quote": "do not use git reset --hard here"
        },
        {
          "text": "kubectl scale deploy/api --replicas=3",
          "expect": "fires",
          "gates": [
            "deploy"
          ],
          "quote": "kubectl scale deploy/api --replicas=3"
        },
        {
          "text": "git filter-branch --tree-filter rm -rf secrets",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "git filter-branch --tree-filter rm -rf secrets"
        }
      ],
      "no_shell_parser": [
        {
          "text": "rm -rf \"./_scratch/a|b/../../.claude\"",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf \"./_scratch/a|b/../../.claude\""
        },
        {
          "text": "rm -rf node_modules$(echo /../.claude)",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "rm -rf node_modules$(echo /../.claude)"
        },
        {
          "text": "Get-ChildItem .claude -Recurse | Remove-Item -Force",
          "expect": "fires",
          "gates": [
            "destructive-delete"
          ],
          "quote": "Get-ChildItem .claude -Recurse | Remove-Item -Force"
        },
        {
          "text": "rm -rf node_modules;",
          "expect": "silent",
          "quote": "rm -rf node_modules;"
        }
      ]
    }
  }
}

---
description: Forge V2 — project-aware, memory-aware orchestrator. Classify a task, read project memory, build a task-sized ECC agent team, execute in this folder, log real events to the dashboard, update memory + agent ledger, optionally Codex-review, and deliver an honest report. No mandatory security gates; project isolation always on.
argument-hint: [task | config [list|get <key>|set <key> <value>|unset <key>|reset|explain <key>] | dashboard | legacy dashboard | start | use | status | runs | open-report | resume <run_id> | learn | remember <text> | snapshot | paperclip <goal>]
---

# /forge

Input: **$ARGUMENTS**. Obey `CLAUDE.md` (project isolation + honesty + no fake claims). Work only in this project folder.

## Sub-commands & natural-language triggers (check $ARGUMENTS first)
Also trigger on plain language: "use Forge system", "gebruik Forge systeem", "start Forge dashboard", "open Forge dashboard", "gebruik forge voor dit project". Settings phrases ("instellingen", "settings", "zet … aan/uit/op N%") go to **`config`** below; "hoe vraag ik Forge iets" / "help me ask" goes to **`help me ask`** below.
- **`dashboard`** / `start Forge dashboard` → confirm the active project folder, then find the Command Center — **the ONE dashboard for every project** (owner decision 2026-07-31; it auto-discovers projects, so it does not need to live in THIS folder): (1) if `command-center/gateway/bin.mjs` exists in this project, that IS the Command Center home — if `command-center/dashboard/dist` is missing (never built), build it yourself (`npm run build` inside `command-center/dashboard`) when `command-center/dashboard/node_modules` exists, otherwise say in one line that the dashboard is not built here and continue — never print a build command for the user; prefer the supervisor for long sessions (`node command-center/gateway/supervisor.mjs` — auto-restarts the gateway) or start the gateway directly (`node command-center/gateway/bin.mjs`, port 4100); (2) if it does not live here, health-check the central instance anyway — `http://127.0.0.1:4100/api/health` answering means this project is already covered. Either way: **health-check** `http://127.0.0.1:4100/api/health` and report **`http://127.0.0.1:4100`** as the dashboard URL — **never claim it is running unless that check passed; never fake a start**. **Do NOT auto-start the retired per-project Control Center as a fallback** — `.claude/forge-dashboard/server.cjs` is RETIRED (never started automatically in any project); only `log-event.cjs` in that directory stays in active service as the per-project run-event writer, completely unchanged. If no Command Center is reachable anywhere, say so honestly in one line (with the expected URL) and continue — Forge starts what it can itself and never hands the user a command to type. The usage guard is not started by this sub-command — BUILD BY DEFAULT starts it when config `usage-guard` is on (the tool prints its own disclosure on a real start).
- **`config`** / "instellingen" / "settings" / "zet <iets> aan|uit|op N%" / "vraag me niet meer bij elke fase" / "interview mode aan" / "codex review uit" → the owner's settings: every Forge feature is ON by default, and this lists or changes any of them in one command. Map the phrase to a key yourself — `node .claude/forge-bin/forge-config.cjs parse "<sentence>"` names the exact `set` it would run; when it says "No setting recognised" or exits 3, map it from the schema aliases in `.claude/config/orchestration/FORGE_CONFIG_SCHEMA.json` or offer its candidates as options ("vraag me niet meer bij elke fase" → `set autonomy continue-within-mission` + `set start-gate off`). Then **run the CLI yourself** — `node .claude/forge-bin/forge-config.cjs list|get <key>|set <key> <value>|unset <key>|reset|explain <key>` — never edit `FORGE_CONFIG.json` by hand and never tell the user to run it. Repeat the tool's OK line verbatim; for `list`, print the tool's own table in the owner's language (`--lang nl|en`; add `--ascii` when the output lands in Windows cmd). Add one plain line on what changed and when it takes effect: a `usage-guard` key reaches the running guard only after a restart, so run `node .claude/forge-bin/usage-guard.cjs stop` and then `start` yourself right away (only `stop` when it was switched off); every other key applies from the next step. `reset` without `--yes` exits 3 → ask once ("Alles terug naar de standaard?") and rerun with `--yes`; a hard gate is locked (exit 3) — say in one line that hard gates cannot be switched off.
- **`help me ask`** / "hoe vraag ik Forge iets" / "hoe moet ik dit vragen" → show `.claude/skills/forge-prompt-coach/references/HOW-TO-ASK.md` (the Dutch part first, then English) — just that file, never a lecture.
- **`legacy dashboard`** / `old Forge dashboard` → the RETIRED per-project Control Center, available ONLY on this explicit request (it never starts automatically): check `.claude/forge-dashboard/server.cjs` exists, start it, health-check `http://localhost:<port>/api/health` (port from `.claude/forge-dashboard/PORT`), report that URL, and remind that the Command Center on 4100 is the current dashboard.
- **`start`** → same as `dashboard`, then begin a new run (steps 0–1 below).
- **`interview`** → the full intake questionnaire, one question at a time (`forge-intake` skill §Iteratieve intake). By default `/forge <goal>` answers the intake itself; this is the opt-in for owners who want to be asked.
- **`propose-skill`** / **`approve-skill <name> --owner-approval <token>`** → `node .claude/forge-bin/forge-genesis.cjs propose --gap "<x>" --evidence <file>` stages a skill proposal under `.claude/forge-genesis-staging/`; `approve` (owner token required) promotes it — never automatic.
- **`tournament`** → `node .claude/forge-bin/forge-tournament.cjs` — best-of-N variants scored against a transparent rubric; the winner is promoted, never fabricated.
- **`secondbrain`** → `node .claude/forge-bin/forge-secondbrain.cjs` — read-only, secrets-excluded portfolio strategist; every recommendation cites `{project, file, fact}`.
- **`codemodel`** → `node .claude/forge-bin/forge-codemodel.cjs` — incrementally updated repo index; results are labelled stale when the index is behind.
- **`briefing <run_id>`** → `node .claude/forge-bin/forge-briefing.cjs <run_id>` — the morning briefing for an overnight (`forge-nightshift`) run.
  *(These six were advertised by `forge-core` since v8.1 but had no entry here; wired 2026-09-23 after an external audit. Each tool ships in `forge-bin/` and prints usage on `--help`.)*
- **`use`** / `use Forge system` / `gebruik Forge systeem` → load project memory/profile; if Forge isn't installed in this folder, install it yourself exactly like the bare-folder step in BUILD BY DEFAULT (or say in one line that Forge is not installed on this machine when its template is absent too); start the dashboard yourself as in `dashboard` above (or say in one line that it is not installed here); then continue the task via the flow below.
- **`status`** → read `.claude/FORGE_MEMORY.md` + `.claude/forge-dashboard/DASHBOARD_STATE.json` + newest `.claude/forge-runs/*/run.json`; summarize project status, latest run, and dashboard URL.
- **`runs`** → list THIS project's `.claude/forge-runs/*/` newest-first.
- **`resume <run_id>`** (WAVE D) → a session/usage limit killed a swarm mid-run; reconcile that run's manifest **purely from its logged `events.jsonl` content** (never inferred from side effects, never a fabricated "done") and re-dispatch **only** the still-unfinished work packages with their original narrowed scope. Requires the run to have been ARMED at dispatch time — that ARM step is now `arm-manifest` in `.claude/config/orchestration/run-checklist.json` (advisory, sits between `route` and `dispatch`): `node .claude/forge-bin/forge-manifest.cjs arm --run <run_id> --wps <file.json> --log-event` — one record per WP: `wp_id, agent, narrowed_prompt, deps?`. **`--log-event` logs the `manifest_armed` proof event in the SAME act as the write** (2026-08-01), so arming and its proof can no longer disagree; without it you must log `manifest_armed` yourself or `forge-orchestrate.cjs audit` will report the step skipped. Once a run is armed, `node .claude/forge-bin/forge-manifest.cjs ready --run <run_id>` gives the honest frontier and `... waves --run <run_id>` gives the full dispatch order in which everything inside one wave is mutually independent — that is the mechanical way to satisfy the owner's "one writer per hotspot at a time" HARD MUST instead of eyeballing the order. Then: `node .claude/forge-bin/forge-swarm-resume.cjs --run <run_id> [--json]` (or `forge.cmd resume --run <run_id>` / `forge.ps1 resume --run <run_id>` / `bash forge.sh resume --run <run_id>`) — exit 0 = complete (nothing to resume) / 3 = resumable / 2 = usage error or no manifest was ever armed. Re-dispatch each returned unfinished WP exactly as originally scoped and log `wp_resumed` per WP as it goes back out. A `done` WP is **never** re-dispatched. This module has zero side effects of its own beyond persisting the reconciled manifest — a WP whose own work is side-effecting (email/deploy/payment/etc.) still needs its own `forge-checkpoint.cjs` idempotency guard before repeating that side effect.
- **`open-report`** → print THIS project's newest `.claude/forge-runs/*/final-report.md`.
- **`learn`** (WAVE F, opt-in, read-only) → **cross-project learning harvest**: reads OTHER Forge projects' `.claude/FORGE_*.md` memory files (never writes to them) and stores real, evidenced lines from them into THIS project's reserved `global` lesson namespace — the same namespace `forge-recall.cjs` always blends into every dispatch, so a lesson from project A can now surface as advisory guidance in project B. `node .claude/forge-bin/forge-harvest.cjs --scan <portfolio-dir> [--global-store <file>] [--dry-run] [--json]` (or `--projects <a,b,...>` for an explicit list; or `forge.cmd learn` / `forge.ps1 learn` / `bash forge.sh learn`, passthrough args). **Explicit discovery only** — neither flag given means nothing is scanned; `--scan <dir>` only checks that dir's IMMEDIATE child folders for a `.claude/FORGE_*` marker (one level, never recursive, never a whole-disk walk). **Secrets/PII excluded**: `.env`/`.env.*`/`*.key`/`*.pem`/`*secret*`/`*credential*`/`id_rsa*` are never opened, and every line read is redacted (reusing the same secret-pattern set the leak scan uses) before it can become a lesson — a line that still looks secret-shaped after redaction is dropped outright, never stored. **Evidenced-only**: a stored lesson is a verbatim quote from a real logged line (a decision-log row, a "what worked" note, a recurring owner-ask) tagged with its source project/file/line — never synthesised or summarised into a new claim; un-recorded history yields no lesson. Always **read-only on other projects** — its only write is to this project's own global lesson store. Use `--dry-run` first to see what WOULD be learned without writing anything. Log `lessons_harvested` (projects scanned, lessons stored, skipped-secret, skipped-non-canonical) after a real (non-dry-run) harvest.
- **`remember <text>`** / "onthoud dat …" (WAVE B / B4) → promote an explicit owner instruction into a real, ACTIVE standing rule: `node .claude/forge-bin/forge-standing.cjs remember "<text>" [--scope <scope>] [--trigger always|domain|glob|on-request] [--domain <domain>] [--glob <glob>] [--topic <topic>]`. This is the **ONLY** sanctioned path that writes a new `status:"active"` rule — it stamps `source:"owner /forge remember"` (never a fabricated/inferred source) and can never mint a new `cannot_override_core` rule. Report the written rule id + text back to the owner.
- **`snapshot`** (owner request 2026-07-29, `.claude/skills/forge-snapshot/SKILL.md`) → manual/phase-boundary context-continuity snapshot: `node .claude/forge-bin/forge-snapshot.cjs write --reason phase` regenerates `.claude/FORGE_SNAPSHOT.md` from real sources (memory/task-history/work-packages/decisions/git/latest run events/doctor) — Mission preserved verbatim, every other section re-derived fresh, never a fabricated context percentage. This is also fired **automatically** by the wired PreCompact (matchers `manual`+`auto`) hook and re-injected on the following SessionStart (matcher `compact`) — see `HOOKS_OPT_IN.md` section 4 for the live wiring record. `node .claude/forge-bin/forge-snapshot.cjs check --max-age-hours <n>` reports staleness.

## Forge Session Mode (project-local)
`/forge`, `gebruik Forge`, `gebruik Forge systeem`, `use Forge`, `start Forge` (or `start Forge session`) → enter **Forge Session Mode** for THIS project: set `.claude/FORGE_SESSION_STATE.json` `mode:"on"` (+ `since`). While ON, treat **follow-up prompts in this project as part of the current Forge session** — automatically run them through the Forge flow (Lead Agent + dynamic subagents); the user need not repeat "gebruik Forge"; keep dashboard/memory/task-history/ledger updating. **Pause/stop:** `stop Forge`, `pause Forge`, `normale Claude mode`, `doe dit zonder Forge` → set `mode:"paused"`/`"off"`. **Status:** `Forge status` / `is Forge actief?` → read + report `FORGE_SESSION_STATE.json`. Session Mode grants **no extra permission**: project isolation stays ON, ECC Normal ON, ECC Full Test OFF (unless explicitly enabled), and no credentials/live-workflows/production deploy/global config/external writes without explicit approval. The dashboard shows a **FORGE SESSION** badge from this file.

**BUILD BY DEFAULT (beginner-first — external audit 2026-09-23 replaced the old ARM → START gate; config-driven since v2.7.0, 2026-09-24).** Do the setup first, but never stop for it. Each step below follows an owner setting — read it with `node .claude/forge-bin/forge-config.cjs get <key>`; everything is ON by default and `/forge config` changes it. In order: **target folder** — the current working directory; state it in the first line and never ask which folder when the cwd is a project (writing outside the root is already a hard gate); if the cwd is the user's home folder or a drive root, refuse in one plain line ("Forge werkt alleen in een projectmap — maak een map en open Claude daar") and stop → **goal intake** (config `intake`: `silent` = answer the intake questions yourself from the mission text, the project scan, `.claude/.forge-setup.json` — written by `/setup-forge`, never re-ask what it already holds — and `FORGE_PROJECT_PROFILE.md`, recorded in the PRD under *Assumptions (auto-filled)*; `interview` = the `/forge interview` flow). With config `prompt-doctor` on, first run `node .claude/forge-bin/forge-promptcheck.cjs ask "<raw request>"` on the raw request: exit 3 → ask exactly its `nextQuestion` (options A/B/C + "iets anders", A recommended) as the ONE allowed question; otherwise record its `assumptions` under *Assumptions (auto-filled)*. Ask the owner at most ONE question, chosen by the `forge-prompt-coach` skill (only when two readings lead to materially different builds, or for an outward/irreversible action), as 2–3 plain-language options + iets anders, then confirm in one sentence (Ik bouw dus: … Klopt dat?) → **dashboard** (config `dashboard` on): `GET http://127.0.0.1:4100/api/health`; if it fails and `command-center/gateway/supervisor.mjs` exists in this project, start it yourself in the background and re-check for 5 s; if it does not exist, post one line `Dashboard not installed here (optional) — continuing without it` — **never print a start or build command for the user to type** → **usage guard** (config `usage-guard` on): run `node .claude/forge-bin/usage-guard.cjs start` yourself; the tool prints its own disclosure only when a new watcher really starts (repeat those lines once), says "already running" otherwise, and exits 3 without starting anything when the owner switched it off → run_id (`run.json` status `"running"`) → **git checkpoint** (config `git-checkpoint` on, an L2+ build or any destructive request, and `git status --porcelain` not empty), in this order: (1) `node .claude/forge-bin/forge-setup.cjs gitignore` — append-only, it makes `.gitignore` keep every secret-shaped name out of git BEFORE anything is staged; (2) `git add -A`; (3) `git diff --cached --name-only`, and unstage every secret-shaped name still in that list — `.env*` except `.env.example`, `*.pem`, `*.key`, `id_rsa*`, `credentials*.json`, anything under `secrets/` — with `git restore --staged -- <file>` (`git rm --cached -q -- <file>` in a repo without a first commit; the file stays on disk), saying in one line which file was left out and why; (4) `git commit -m "forge: checkpoint before <run_id>"` on the current branch (or on a `forge/checkpoint-<run_id>` branch when the owner's branch must stay clean) — local only, NEVER pushed; log it as `command_run`; not a git repo → say so in one line and skip → CLAUDE.md + Mission Blueprint + **PRD for any L2+ build** (`node .claude/forge-bin/forge-prd.cjs write '<prd-json>' --run <run_id> --tickets` — one ticket per acceptance criterion; L1 one-file fixes and pure research/review runs skip it and say so) + work packages → **contract preview** (`node .claude/forge-bin/forge-runcontract.cjs check --run <run_id>` — expect NOT DONE now; you now KNOW the rules this run must satisfy) → subagent roster (never more than config `team-max` Bosses at once; with config `model-tiering` on, pick each subagent's model per MODEL ROUTING below). **Destructive words** in the request ("opruimen", "verwijder", "clean up", "delete", "remove", "wipe") → the plan lists exactly what will be deleted — the keep-list first — and the git checkpoint above is mandatory before it; never "clean up" a folder wholesale. Then post exactly one line — `Plan ready — <n> work packages · team: <bosses> · Building now — say STOP to pause.` — and **continue straight into building**. **START gate = config `start-gate`:** `off` (default) = never wait for START; `l4-only` = wait for an explicit START only on an **L4** (phased) mission; `always` = wait on every build. "wait", "ask first" or "plan only" from the owner always win for that run, and STOP always works. With config `explain-mode` on, give each phase ONE plain-language sentence ("Wat ik nu doe en waarom: …") — never more. **"Smallest team" is about team SIZE, and the ceremony scales with the level:** an L1 one-file fix may run as ONE Boss with ONE verify pass; L2+ always gets plan + subagents + verify. Bare folder (no `.claude/forge-bin/`)? Copy `~/.claude/forge/template/.claude` into `./.claude` NOW (touches only `.claude/`), then run `node .claude/forge-bin/forge-setup.cjs gitignore` so secret-shaped files are kept out of git from the start, say in one line what was installed, and continue — never merely offer; if that template is absent, say so in one plain line (Forge itself is not installed on this machine; its installers are `install.sh` / `install.ps1`). **NEVER author/redesign your own dashboard — always COPY the canonical one** (a different self-made dashboard is a FAILURE). The hard gates — deploy, push, spend, DNS, production, credentials, activating a live workflow, sending anything out, killing processes by name, destructive deletes, git commands that throw away uncommitted work, writing outside the project — are unchanged and always interrupt.

**BEGINNER PROMISE (owner directive 2026-09-24).** Forge does it for you. It runs every command, script, install and build itself and never asks the user to run a file or code — no "run this in your terminal", no "open PowerShell and type …"; when a step truly needs the person (logging in, a paid key, an approval), it names exactly that one thing in plain words. It never asks "shall I continue?" / "moet ik verder?" between phases (autonomy `continue-within-mission`, `start-gate off`, standing rule `does-it-for-you`). The ONLY things it always stops for are the hard gates — deploy, push, spend, DNS, production, credentials, activating a live workflow, sending anything out, killing processes by name, destructive deletes, git commands that throw away uncommitted work, writing outside the project — and a real usage-limit pause (`node .claude/forge-bin/forge-autonomy.cjs decide "<phase>" --phase --live`). Everything is ON by default, and `/forge config` lists every setting with its value and changes any of them in one command.

**GATE HOOK — the yes-path (config `gate-hook`, default on).** A PreToolUse hook (`.claude/forge-bin/forge-gate-hook.cjs`) really stops three kinds of command before they run: destructive deletes, killing processes by name, and git commands that throw away uncommitted work. When it blocks a command (exit 2), Forge (1) says in one plain sentence exactly what would have been deleted, killed or discarded, and offers the safe variant first (move to a dated backup folder instead of deleting, stop the one process by its exact PID, commit or stash before discarding); (2) only when the owner explicitly answers yes to THAT command, runs `node .claude/forge-bin/forge-config.cjs set gate-hook off --once "<the owner's words>"` itself, runs the approved command once, then runs `node .claude/forge-bin/forge-config.cjs set gate-hook on` and checks in the same turn that `node .claude/forge-bin/forge-config.cjs get gate-hook` shows `on` again — telling the owner that the one-off also switches itself back on after 10 minutes; (3) never switches the hook off without `--once`, never reuses one yes for a second command, and never asks the owner to run the command themselves; (4) logs `owner_override` with the owner's exact words (`{"gate":"<gate id>","quote":"<the owner's words>","command":"<what ran>"}`).

**VERIFY-AND-CORRECT + SCREENSHOT PROOF (per task).** After each meaningful task: prove the claim is real (file exists / test actually passed / agent actually ran / module renders) before logging done or telling the user it works. Where visual, screenshot-loop the app view, the Forge dashboard FLOW, and (when Paperclip is used) the Paperclip agent tabs to confirm reality (agents have real instructions+skills+runs, no open "complete" gaps). Mismatch → fix if clearly fixable in scope, else surface as a blocker. See forge-core "PER-TASK VERIFY-AND-CORRECT + SCREENSHOT-LOOP PROOF".

**MODEL ROUTING (quota tiering).** Config `model-tiering` (default on) switches this on; off = every subagent runs on the session's own model. Build the team model-tiered: Lead/orchestration on Opus (recommend the owner run `/model opusplan`); default subagents to `sonnet`; escalate only the highest-stakes subagents to `opus` (the Lead decides — but security/auth/architecture/migrations/final-verdict ALWAYS Opus); use `haiku` only for the simplest tasks (classify, summarize, memory/ledger/event writes, formatting); pure mechanical edits use `Edit` with no subagent. Set each subagent’s model via the Agent-tool `model` param and log the ACTUAL model used. Read `.claude/FORGE_MODEL_ROUTING.json`. See forge-core "MODEL ROUTING & QUOTA TIERING". Forge cannot auto-switch the session model — only recommend.

**PERMANENT BOSSES + QA LOOP + NVIDIA ROUTING.** Teams use the 12 FIXED Bosses (`.claude/config/agents/agent-registry.json` — Boss, Head Chef, Review/Test/UI/SEO/Security/Skill/Search/Build/Integration/Docs Boss); never ad-hoc names; small task = fewer Bosses, same names. Skills auto-attach (core map + project-type bundle). Enforce the fix-loop: subagent self-review → Test Boss → domain Bosses → Review Boss; fail → failure report → Boss → Head Chef → fix → re-test (bounded). For in-task LLM work call NVIDIA via `.claude/forge-bin/nvidia-provider.cjs` per `.claude/config/models/model-capability-matrix.json` (fit first; a mock is never presented as real; key only from `.env`/`~/.claude/nvidia.env`, always masked). See forge-core "PERMANENT BOSSES + QA FIX-LOOP + NVIDIA-FIRST MODEL ROUTING" + `.claude/docs/model-routing.md`.

**GOAL INTAKE + PLAN (every `gebruik Forge`/`use Forge`, even outside plan mode — silent by default since 2026-09-23, see BUILD BY DEFAULT above).** Before major work: (1) answer the intake yourself from the mission text, the project scan, `.claude/.forge-setup.json` and the profile, and record those answers as *Assumptions (auto-filled)*; ask the owner at most ONE question (AskUserQuestion), chosen by the `forge-prompt-coach` skill (only when two readings lead to materially different builds, or for an outward/irreversible action), as 2–3 plain-language options + iets anders, then confirm in one sentence (Ik bouw dus: … Klopt dat?) — the full questionnaire is `/forge interview` (or config `intake: interview`); (2) write/refresh the plan (Mission Blueprint) and the project **CLAUDE.md** (safe-merge); (3) record the goal in the blueprint + `run.json`. Never skip planning because plan mode is off.

**PAPERCLIP — OPT-IN ONLY (decoupled from `gebruik forge`; user decision 2026-07-04, supersedes the 2026-07-02 auto-provision rule).** Config `paperclip` (default `off`) is this same rule, now enforced by the bridge: while it is `off`, `forge-paperclip.cjs up` and `ensure` refuse with exit 3 — so when the owner EXPLICITLY asks for Paperclip (`gebruik paperclip`, `start paperclip`, `/forge paperclip <goal>`), run `up`/`ensure` with `--force` (the explicit ask is the consent), and otherwise never start it; `on` (`/forge config set paperclip aan`) = the owner opted in for this project, so `up`/`ensure` run without `--force` (same bridge, same guards). `gebruik forge` runs on the **Forge dashboard alone** — do **NOT** start the Paperclip runtime, do **NOT** provision companies/agents/tickets, do **NOT** require Paperclip for any project type (cashflow included). Paperclip remains available as a **separate plugin**, only when the user explicitly asks (e.g. `gebruik paperclip`, `start paperclip`, `/forge paperclip <goal>`). When (and only when) invoked, use the project-local bridge `.claude/forge-bin/forge-paperclip.cjs` with the existing flow (`up`/`ensure` take `--force` while config `paperclip` is off — see above) — mapping: **1 Forge project = 1 Paperclip company** (nothing mixes):
1. `up` — start/reuse the loopback runtime (127.0.0.1:3100; isolated `PAPERCLIP_HOME`); report the real URL.
2. `ensure --run <run_id> --goal "<goal>" --agents .claude/FORGE_PAPERCLIP_AGENTS.json` — idempotent: company (= project name) · **goal** · project + **workspace bound to the exact project folder** · **agents from the role map** (role enum: ceo/cto/cmo/cfo/security/engineer/designer/pm/qa/devops/researcher/general; executing agents = `claude_local` with the durable standalone path; thinking roles = `process`) · per agent **instruction docs in `docs/agents/<slug>/AGENTS.md + SOUL.md + TOOLS.md`** · binding in `.claude/FORGE_PAPERCLIP_BINDING.json`.
3. `ticket --run <run_id> --title "WP-xx: …" --agent <slug>` per work package.
4. **Every step is auto-logged to the Forge dashboard** (`paperclip_*` events) — Paperclip and the Control Center stay ONE world; an empty dashboard while Paperclip works = blocker.
5. Guards stay on: loopback only · git-init before claude_local writes · no comment/self-wakes configured · `pause` agents after proof (dashboard stays UP; `stop` only when fully done or the user asks) · no credentials · no Codex write.
If the runtime can't start, log `paperclip_runtime_blocked` + report honestly — never pretend the control plane is active. **Never mark Paperclip BLOCKED/missing in reports when it simply wasn't requested — it is opt-in, absence is normal.**

**On install / first activation, maintain the project's CLAUDE.md + skills (Forge maintains four things: CLAUDE.md · `.claude/skills/*` · memory · dashboard).** Check `CLAUDE.md` (log `claude_md_checked`): missing → create from the project scan (log `claude_md_created`); exists → read it, **preserve all existing rules, and add/refresh only a clearly-marked `## Forge Studio v7` section** (log `claude_md_updated`) — never overwrite blindly; on a real conflict log `claude_md_conflict_detected` and ask. Check `.claude/skills/` (log `project_skill_dir_checked`; create if missing) and create project-local custom skills as needed (`custom_skill_created` → `custom_skill_used` when a subagent runs one). Maintain the **skill registry** `.claude/FORGE_SKILL_REGISTRY.md` (`skill_registry_checked`/`_created`/`_updated`). Never touch global skill folders without explicit approval. The dashboard shows **CLAUDE.md** + **Custom Skills** + **Codex** + **Browser Proof** status. For a **fresh-install verification** (on request), verify CLAUDE.md/skills/registry/memory/session/dashboard + ECC Normal ON·Full Test OFF + project-local-only → `artifacts/fresh-install-verification.md`.

Terminal equivalents — optional, for people who like a terminal; Forge never needs you to run these (project-local wrappers in `.claude/forge-bin/`, no global install): PowerShell `.\.claude\forge-bin\forge-dashboard.ps1` · CMD `.claude\forge-bin\forge-dashboard.cmd` · Bash `bash .claude/forge-bin/forge-dashboard.sh`. Same pattern for `forge-status` / `forge-runs` / `forge-open-report`, or the dispatcher `forge.ps1 <cmd>` (Forge defines no npm scripts — the wrappers are the whole terminal interface).

Project isolation: only ever touch the active project's `.claude/`; never read/start another project's dashboard, port, or state. If the folder is genuinely ambiguous (the cwd is not a project and was not refused as a home folder or drive root — see BUILD BY DEFAULT), ask first.

Otherwise treat $ARGUMENTS as a task and run the **Lead-Agent Swarm Execution Loop** below. You are the **Lead Agent / Orchestrator**: you don't just talk — you decompose into real executable subagent work packages, run them, merge, review, fix-loop, and deliver. Log only **real** events with `node .claude/forge-dashboard/log-event.cjs <run_id> <event_type> "<json>"`. The Control Center renders these live (FLOW = Lead → Subagents → Merge → Codex → Final). For the full prompt format see `.claude/skills/forge-router/PROMPT_TEMPLATE.md`.

**ECC-first (ECC Normal Mode — default ON).** Forge is ECC-based. Read `.claude/FORGE_ECC_MODE.json` (default `ecc_normal_mode:"on"`, `ecc_full_test_mode:"off"`). In Normal Mode: prefer real **ECC agents/skills** over native/internal roles — list the ECC inventory, select the relevant ones, **really try to invoke them**, tag each work package + agent event with a **`runtime`** (`ecc-agent` · `ecc-skill` · `native` · `codex` · `internal`), and report honestly what ECC did. Native is only a **labeled fallback** (`runtime:"native"` + `native_fallback_used` event) when ECC is unavailable/blocked/failed/too-small. Never present native as ECC. **ECC Full Test Mode** (broad inventory / unblock-diagnose / permission test) runs only when config `ecc-full-test` is on — after the user says `enable ECC test mode for this project`, run `node .claude/forge-bin/forge-config.cjs set ecc-full-test on` yourself (the same as `/forge config set ecc-full-test on`), which forge-config bridges into `ecc_full_test_mode` in `.claude/FORGE_ECC_MODE.json`; default OFF. The old `.claude/ECC_TEST_MODE.md` marker is legacy and no longer required. Normal Mode still respects project isolation + approval gates (active folder only; no other projects; no global config; no credential/live-workflow/production/external writes without explicit approval).

## 0. Owner Prefs, Standing Rules & Autonomy (required, before Mission Intake) — WAVE B / B4
Apply `.claude/config/orchestration/precedence.md`'s ordering (hard-gates > current owner instruction > standing rules + owner config > owner-profile defaults > auto-defaults) BEFORE intake: read owner-profile defaults (`.claude/forge-bin/forge-prefs.cjs::resolve()`), active standing rules for this run (`.claude/forge-bin/forge-standing.cjs::match()`), and the autonomy mode (`.claude/forge-bin/forge-autonomy.cjs::decide()` — the current instruction > config `autonomy` > the default, always deferring to the live hard-gate classifier first; `decide "<phase>" --phase --live` also stops on a real usage-limit pause). Once `run_id` is assigned in step 1, **first** run `node .claude/forge-bin/forge-config.cjs diff --run <run_id> --mark-seen`: exit 3 = an owner setting changed since the last run (it logged ONE `config_changed` event) → repeat its lines to the owner in your first message ("Je hebt usage-guard.pause-at op 97 gezet — toegepast.") before continuing; exit 0 = nothing changed; exit 2 = a `FORGE_CONFIG.json` is damaged (the message names the file) → tell the owner in one line which file is damaged and that Forge runs with the safe defaults until it is fixed — every feature that uses their data, the network or money (usage-guard, nvidia, codex-review, mcp, portfolio, paperclip; cleanup stays report-only) is off, everything else at its default — or reset with `/forge config reset --yes` (only after the owner's yes: it keeps the damaged file as a `.damaged-<time>` backup and drops the saved settings; the gate hook treats a reset as a gate change, so it goes through the GATE HOOK yes-path), then continue the run. **Then** compose and log the applied-prefs ECHO: `node .claude/forge-bin/forge-echo.cjs emit <run_id>` — logs one `owner_prefs_loaded` event summarizing exactly which prefs/rules applied and the resolved owner config, so this ordering is visible on the dashboard, not just documented. See `.claude/skills/forge-router/SKILL.md` "Step 0-owner" for the full detail. A staged `FORGE_PREF_CANDIDATES.json` entry is never auto-active; the owner's `/forge remember <text>` (above) is the only sanctioned path that writes a new active standing rule.

## 1. Mission Intake
`run_id = forge-YYYY-MM-DD-HHMMSS` (real time). Write `.claude/forge-runs/<run_id>/run.json` (request, mode, project_type, stack, complexity, quality_target, status:"running", project_folder). Log `run_started`. Then run the config diff and emit the owner_prefs_loaded echo (see step 0) now that `run_id` exists.

**Real-fixtures intake (WAVE D, correctness-critical domains only).** If the detected project/domain is finance/invoicing, a parser, OCR, a data/ETL transform, or prediction/backtesting, check `node .claude/forge-bin/forge-fixtures.cjs check --domain <finance|parser|ocr|data|prediction> --fixtures <a,b,...>` (or `--waiver "<reason>"` when the owner has explicitly accepted the gap) BEFORE that work can honestly be called done — see `.claude/forge-fixtures/README.md` for the vault (real, owner-provided samples only, never committed). No real fixtures and no waiver = exit 3 BLOCKED (log `fixtures_required`), not a silent synthetic fallback. An explicit logged waiver unblocks it but stays visibly flagged (log `fixtures_waived`), never silent. Non-critical domains are never blocked.

## 2. Deep Prompt Analysis (load memory + ECC inventory)
Read `.claude/FORGE_PROJECT_PROFILE.md` · `FORGE_MEMORY.md` · `FORGE_TASK_HISTORY.md` · `FORGE_ECC_MODE.json` + relevant project files. Detect project type/stack. **Take the ECC inventory** (which ECC agents/skills/commands are available for this task) and log `ecc_inventory` (status `on`/`blocked` + what's available, or the reason ECC is unavailable). Log `project_scanned` · `profile_loaded` · `memory_loaded` · `agent_decision_summary`. Never fake project knowledge or ECC availability — mark `inferred`/`unknown`.

## 3. Mission Blueprint / Master Plan (the Lead Agent plans BEFORE executing)
Don't execute randomly first. As **Lead Agent**, write `.claude/forge-runs/<run_id>/mission-blueprint.md` (or `mission-packet.md`); log `mission_blueprint_created` (`expanded_mission`). The blueprint MUST include: original prompt · improved/expanded mission · **detected project type** (create a **custom taxonomy** if it's outside the usual website/n8n/RAG/scraping/fullstack/prediction set — never force-fit) · detected technologies/tools · detected project state · quality target · assumptions · constraints · **do-not-touch list** · role groups needed · selected agents · custom agents created · why each agent (and rejected agents + why) · work-package breakdown · dependencies · expected artifacts · review/rework/test strategy · definition of done · dashboard layout plan · **a Skill Discovery section (below)**.

### 3a. Skill Discovery (required before assigning subagents) — log `skill_discovery` then `skill_map_created`
The Lead Agent has **final control over skill selection**; the playbook skill lists are **examples, not limits**. Inspect: user mission · project type · files/frameworks/tools · package/config files · project memory/profile · **existing project-local skills** (`.claude/skills/`) · **global Forge skills** · **available ECC agents/skills** · available MCP/tools · quality target · required outputs · risks/dependencies. The blueprint's **## Skill Discovery** records: detected project type · detected technologies/tools · required capability areas · available skills found · skills selected · skills assigned to subagents · skills not selected + why · missing skills · custom skills needed · fallback plan. **If a needed skill doesn't exist, do not ignore the gap** — choose one: (1) create a project-local custom skill under `.claude/skills/<name>/SKILL.md` (only when safe, scoped, documented, project-local, and linked to a real subagent/work package) and log `custom_skill_created`; (2) write a custom-skill plan/scaffold for user approval; (3) assign a **native fallback method (labeled honestly)**; (4) mark the work **BLOCKED** if no safe method exists. For a genuine, evidenced capability gap worth a durable evidence-tracked proposal (not a one-off), use `node .claude/forge-bin/forge-genesis.cjs propose --gap "<x>" --evidence <file>` instead of (1) — see `.claude/skills/forge-router/SKILL.md` Step 0b for the full contract (staged-only, never live until an explicit owner `approve --owner-approval <token>`). **No executable subagent runs without an assigned skill/method/capability route.**

## 4. Dynamic Role-Based Subagent Selection → load skill `forge-router`
The target folder is the cwd (BUILD BY DEFAULT — never ask when it is a project; a home folder or drive root is refused). Classify L1–L4. The Lead Agent **decides the roles** — but **team names come from the permanent Boss registry** (`.claude/config/agents/agent-registry.json`): map each required work area to the covering Boss (+ its ECC agents/skills, ECC-first). A genuinely new role is allowed ONLY per the registry's `extensionRule` (permanent standardized name + role + skill bundle + model mapping + self-review + loop position, added template-first) — never ad-hoc/random names for work a Boss covers. `forge-router` returns the **smallest useful but powerful** team + matching playbook. Log `role_map_created`, then `agent_selected` per agent (with `runtime`). For each **custom** subagent log `custom_subagent_created` with: name · role · why needed · project evidence · mission · inputs · allowed/not-allowed · expected output + artifact path · evidence required · handoff · success/rework criteria · runtime target · status. Show the plan in one line and continue building (BUILD BY DEFAULT); wait for an explicit go-ahead only when config `start-gate` asks for it (`l4-only` → an **L4** mission, `always` → every build), when the owner wrote "wait"/"ask first"/"plan only", or when a hard gate is in play.

## 5. Assign Subagent Work Packages
For **each** selected/custom subagent log `agent_work_package_created` (custom roles: `custom_subagent_created`, `custom:true`) with: `agent, role, runtime, mission, inputs[], allowed_actions[], not_allowed[], output_artifact, evidence_required[], handoff, success_criteria, rework_criteria, status:"previewing"` **plus the assigned `skill` + `skill_source`** (`ecc-skill`/`forge-skill`/`project-local`/`native`/`internal`/`unavailable`) — log `skill_assigned` if assigned separately. Archive `.claude/forge-runs/<run_id>/work-packages.md`. (`previewing` → node previews **blue** until the agent really starts.) Subagents must do **different** parts — not all the same general analysis.

## 6. Parallel Subagent Execution (this folder only) — ECC-first
**CONTRACT-FIRST (required before any parallel builders that share interfaces).** Write ONE authoritative contract (`docs/ARCHITECTURE.md` and/or `src/contract.js`) pinning the LITERAL shared details — exact action/message shapes + payload key names + nesting, exact function/API names+signatures, exact DOM ids AND CSS class names/tokens, file ownership (one owner per file), and the component lifecycle (`mount()` returns a cleanup the host calls on unmount/switch). Embed each builder's slice in its prompt; builders code against it VERBATIM (no inventing action/class names). No parallel build without the contract. **INTEGRATION GATE before "complete":** run the ASSEMBLED product headless — boots with no console errors, cross-module actions round-trip, every view/panel mounts, one real user-flow passes. A failure is a BLOCKER, not "done". Green unit tests + `node --check` are NOT enough (they miss cross-module mismatches). Every planned module/view must have an owner. See forge-core "CONTRACT-FIRST BUILDS + INTEGRATION GATE".

**REAL DISPATCH OR HONEST INTERNAL (hard rule — deep-scan 2026-07-07).** A `subagent_started/completed` event MUST correspond to a REAL Agent-tool dispatch (`Agent({subagent_type, name:"<Boss>", …})` or Workflow `agent()`) — pass the Agent tool_use id as `dispatch_id` on that subagent's events. **If YOU (the main session) do the work yourself, do NOT emit a separate named subagent — log it under the Lead/Boss node with `runtime:"internal"` (INTERNAL ROLE ONLY).** Emitting engine-boss/shell-boss/db-boss for work no real subagent did = a fake swarm; `log-event.cjs` stamps it `UNREGISTERED`/`UNVERIFIED` and the dashboard flags it. The `agent` field MUST be one of the 12 registry Bosses (`.claude/config/agents/agent-registry.json`); specialization goes in `role`. Run with `FORGE_STRICT_EVENTS=1` to hard-reject fakes. See forge-core "REAL AGENTS ONLY".

**Really invoke the selected ECC agents/skills** (this is ECC Normal Mode, not a description). Parallel writers → separate git worktrees; you integrate. As work really happens log `subagent_started` (≈ `agent_started`) · `file_read`/`file_changed` · `command_run` · `skill_loaded` · `agent_note` (visible working note, NOT hidden chain-of-thought) · `subagent_output_created` — always with `agent` (a Boss name) + `role` + **`runtime`** (+ `dispatch_id` for a real dispatch). **Fallback:** if an ECC agent/skill is blocked or fails, log `ecc_blocked` / `ecc_agent_failed` (reason), then a native role **only if safe**, logging `native_fallback_used` + `runtime:"native"`. Never silently substitute native for ECC.

**SUBAGENT LIVE LOGGING (required — the dashboard must show WHAT each subagent is doing, not just its .md).** Tool grants are class-based and least-privilege (`.claude/config/agents/agent-tool-policy.json`): only `full-build`/`exec-reviewer` subagents (Build/Test/UI/Integration Boss, codex-reviewer) HAVE Bash and MAY self-log their own progress as they work —
`node .claude/forge-dashboard/log-event.cjs <run_id> agent_progress "{\"agent\":\"<its-name>\",\"role\":\"<role>\",\"runtime\":\"<runtime>\",\"status\":\"running\",\"task\":\"<what I am doing right now>\"}"`
after **each meaningful step** (read inputs · analysis milestone · section written · file created/edited via `file_changed` with real paths · check run), plus ≥1 `agent_note` with visible reasoning and a `subagent_output_created` summary before finishing. `read-only-audit`/`write-no-exec` subagents (Review/Security/SEO/Search Boss, Boss, Head Chef, Docs Boss, Skill Boss — 8 of the 12 permanent Bosses) CANNOT run Bash by design (least-privilege), so for THESE the **Lead backfills their events from the subagent's returned result immediately after it completes, tagged with that subagent's `dispatch_id`** — this backfill is the **DEFAULT path for these classes, not a fallback exception**, and applies only to a subagent that actually ran, never a way to invent one that didn't. Target: **≥3 real activity events per subagent** (self-logged or Lead-backfilled), so the FLOW node shows a live "⚡ doing…" line + progress bar instead of only the artifact. Planned work stays TODO, never faked as done. A running subagent with zero logged activity renders as "⚠ running — no activity logged yet" on the dashboard — treat that as a logging bug and fix it during the run, not after.

## 7. Subagent Outputs / Artifacts
Each subagent produces something real: log `subagent_artifact_created` (≈ `agent_artifact_created`: artifact + path + kind), `agent_handoff`, `subagent_completed`/`agent_failed`. Didn't run → `NOT USED`; thinking-only role → `INTERNAL ROLE ONLY` (status `internal`). No decorative agents/artifacts.

## 8. Lead Review + Rework Loop (bounded)
The Lead Agent **reviews all subagent outputs**: log `lead_review_started` → `lead_review_completed`. For anything incomplete/weak/broken/improvable, log `rework_task_created` (`target`, `issue`, `severity`, `reason`, `required_fix`, `retest_requirement`) → `rework_assigned` → the right subagent fixes (`rework_started`/`fix_started` → `rework_completed`/`fix_completed` with `files_changed`) → `retest_started` → `retest_completed`. **Critical/high must be fixed before final output; medium/minor may be fixed or reported. Max 2 loops default / 3 high-end. Never loop forever**; if blocked, report honestly and ask the user.

## 9. Merge & Synthesis
Collect the reviewed/reworked outputs, resolve conflicts, create the merged result. Log `merge_started` → `merge_completed` + a merged `agent_artifact_created`.

## 10. Codex Review + Unlock (optional, strong for code/architecture/automation/dashboard/refactor/high-end)
**Config `codex-review` decides whether this runs:** `auto` (default) = on important code (auth, payments, data, migrations, large refactors, automation) when the Codex CLI is installed and logged in on this machine — not installed or not logged in → report `NOT AVAILABLE` in one line and continue; `on-request` = only when the owner asks; `off` = skip and report `Codex not invoked (config off)`. When it runs: run `codex-reviewer` (`/codex:review`). Log `codex_review_started` → `codex_finding` per issue → `codex_review_completed`. **If Codex truly runs** → CODEX REAL INVOKED; **if it can't** → run the **Codex unlock diagnosis** (`codex_diagnosis_started`→`_completed`): codex CLI present? version? authed? git repo y/n? interactive/TTY y/n? trust gate? stdout/stderr + exit code + orphan check → write `artifacts/codex-unlock-diagnosis.md` and an **exact manual command** for an interactive terminal (`codex_manual_command_created`: `cd "<folder>"; git status; codex --version; codex exec "<safe prompt>"`) — that command lives in the diagnosis artifact for an owner who wants it; in the chat say in one plain line what blocked Codex and never ask a beginner to run it. Report the exact state — **CODEX REAL INVOKED · BLOCKED: TRUST/TTY · BLOCKED: NO GIT · BLOCKED: NO OUTPUT · NOT AVAILABLE · NOT INVOKED · FALLBACK USED** (events `codex_trust_gate_detected`/`codex_interactive_retry_required`/`codex_retry_started`/`_completed`/`_blocked`). **Never fake Codex or claim proof without real output.** ECC `code-reviewer` may be a labeled fallback.

## 11. Quality Gate → Final Output
Log `quality_gate_passed` when the quality target is met (then `final_output_created`), or `quality_gate_blocked` with a `reason` (`max_iterations_reached` / `unresolved_critical` / `codex_not_invoked`). Report every loop iteration honestly. For a cheap independent second pass, run Claude Code's built-in skills yourself when useful — `/code-review` (diff review in a fresh subagent), `/security-review` (security pass on the pending changes), `/verify` (build-and-run check); nothing to install, and if one is not available in this Claude Code version, say so and use `forge-code-review`.

## 12. Memory Update (always)
Update `FORGE_MEMORY.md` · `FORGE_TASK_HISTORY.md` · `FORGE_AGENT_LEDGER.md` (each row has a **Runtime** + status; statuses ONLY: `ECC REAL INVOKED` · `ECC SKILL LOADED` · `NATIVE AGENT INVOKED` · `INTERNAL ROLE ONLY` · `NOT USED` · `FAILED` · `BLOCKED`; note **custom subagents/skills** created) · `FORGE_DECISIONS.md` on meaningful decisions. Update `FORGE_SESSION_STATE.json` (`last_activity`). Log `memory_updated` / `decision_logged`. **Retention = config `cleanup`:** `report` (default) → `node .claude/forge-bin/forge-retention.cjs scan` only (says what could go, deletes nothing); `auto` → `node .claude/forge-bin/forge-retention.cjs apply` (which itself refuses with exit 3 unless config `cleanup` is `auto` — never pass `--force` without an explicit owner request).

## 13. Dashboard Update + Browser Proof
Events are already streamed live (steps above). If asked to start the dashboard, start it as in `dashboard` above, health-check `http://127.0.0.1:4100/api/health` and report `http://127.0.0.1:4100` only when that check passed — never fake a start. The FLOW lens renders the studio layout (preflight band → mission → lead → master plan → subagents → outputs → **Artifact Collector** (large swarms) → lead review/rework → fix/retest → merge → codex → final), with honest runtime/custom/derived badges, clean category labels + Live/Replay. **Browser proof (v7.1):** if Playwright/Chrome-headless is available, open the dashboard → FLOW → fit → capture → write `artifacts/dashboard-browser-proof.md` (`browser_proof_started`→`browser_screenshot_captured`→`browser_layout_verified`). If unavailable, mark **PARTIAL** with the exact reason (`browser_proof_blocked`) — never claim a screenshot you didn't take.

## 14. Final Report  → load skill `forge-report`
**RUN-CONTRACT GATE (required BEFORE `run_completed` — wired 2026-08-02; before this date the gate existed but had no call site and was evaluated exactly 0 times in 846 real events, which the owner experienced as "agents forget the tasks").** Run `node .claude/forge-bin/forge-runcontract.cjs check --run <run_id> --log-event`. Exit 0 → the `gate_evaluated` proof event is on the run; proceed. Exit 3 (NOT DONE) → the listed missing rules ARE unfinished work: go back and finish them (or obtain an explicit owner override, logged as `owner_override` with a real reason) — **never log `run_completed` over a red contract.**
Write `.claude/forge-runs/<run_id>/final-report.md`; set run.json `status`→`completed`/`failed` + `finished`; log `report_generated` + `run_completed`. Include: blueprint summary, **Skill Discovery** (skills selected/assigned/created/missing), prompt improvements, agents used (incl. **custom subagents** + why) + what each did, artifacts, files read/changed, evidence, **ECC status block**, **Codex status** (the exact v7.1 state + diagnosis/manual-command if blocked), **Browser proof status** (screenshots captured or exact reason unavailable), rework loop (tasks created/fixed), **CLAUDE.md created/updated/no-change/conflict + what was preserved**, **custom SKILL.md files created/updated/used + Skill Registry status**, fresh-install verification (if run), checks run, memory update, dashboard update, session mode, what was **not** done, **strict verdict** (FULL PASS / PASS CORE·PARTIAL PROOF / PARTIAL / BLOCKED / FAIL + per-layer: Core·Codex·Browser·Dashboard·Isolation·Overall), next step. Reflect only checks actually run — **no fake claims**. **For beginners, also:** a plain-language **"Wat is er veranderd"** summary (3–6 short sentences, no jargon: what works now, what changed, what was not done); on a Pro plan (known from `/setup-forge` or the owner — never guessed) one line of model advice (`/model opusplan` or `/model sonnet` — advice only, Forge cannot switch the model); and end every completed run with one line in the owner's language (config `language`): `Volgende taak ongerelateerd? Typ eerst /clear.` (nl) or `Next task unrelated? Type /clear first.` (en)

## Quality Loop (steps 8–11, autonomous)
Lead review → assign rework → subagents fix → retest → merge → (codex) → check quality target. Repeat until: quality target met · max iterations (2 / 3 high-end) · blocked by missing info · user approval required. Each iteration logs its own events and ends with a gate verdict, reported honestly.

## Honesty (hard rule)
`previewing` = work package logged but not executed yet · `INTERNAL ROLE ONLY` = conceptual/thinking role · `derived from event log` = synthetic/un-`agent`-tagged · `Codex not invoked` stated, never synthesized. **No fake agents, custom subagents, custom skills, Codex, tests, files changed, live activity, or progress.** A custom subagent/skill must have a real reason + work package + a real output (or honest BLOCKED). **ECC honesty:** never present a native role as ECC; if ECC was attempted but unavailable/blocked/failed, say so and label the fallback `runtime:"native"`; if ECC genuinely wasn't needed, say that. **Session Mode** grants no extra permission — project isolation/ECC-first/approval gates always apply.

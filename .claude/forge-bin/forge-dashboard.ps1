# Forge — start this project's dashboard (PowerShell). Delegates to forge.ps1 (Node detection). Project-local only.
# If blocked by execution policy, use forge-dashboard.cmd instead.
& "$PSScriptRoot\forge.ps1" dashboard

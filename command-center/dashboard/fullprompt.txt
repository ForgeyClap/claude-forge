/goal /forge

MISSION — TRANSFORM THE APPROVED FORGE WORKSPACE PROTOTYPE INTO A COMPLETE REAL LOCAL FORGE AND CLAUDE CODE WORKSPACE, WITH LIVE USAGE, FILE ATTACHMENTS, PRODUCT IMPROVEMENTS AND ULTIMATE ADVERSARIAL CERTIFICATION

Continue in the current Forge Workspace project.

The visual prototype has been approved.

Now transform it into a complete, genuinely functioning local Forge Workspace connected to:

- the real local Forge runtime;
- the locally authenticated Claude Code installation;
- the existing Claude subscription/session;
- Claude Code used through Visual Studio Code and the local Claude CLI environment;
- real projects;
- real conversations;
- real Claude Code sessions;
- real Boss and subagent activity;
- real skills;
- real tasks and work packages;
- real uploaded files;
- real project files;
- real artifacts;
- real commands;
- real tests;
- real proof;
- real checkpoints;
- real usage telemetry;
- real-time event streaming;
- real crash recovery and resume.

This must not use a separate Anthropic API key.

This must not call the Anthropic Messages API directly.

Claude must be used through the locally authenticated Claude Code environment already available on this computer through Visual Studio Code and the local Claude Code CLI.

The first deployment target is this computer only.

A possible future Mac mini installation and private Wi-Fi/LAN access must remain architecturally possible, but must not be activated now.

==================================================
PRIMARY OBJECTIVE
==================================================

The finished Forge Workspace must allow the user to:

1. Open one local dashboard.
2. View all Forge projects.
3. Create a new project.
4. Open an existing project.
5. Start a new conversation.
6. Resume an earlier conversation.
7. Send a normal natural-language request.
8. Upload or attach files.
9. View live Claude Code usage and session information above the chat.
10. Watch real streamed Claude output.
11. See real Boss, Head Chef, subagent and skill activation.
12. See real work packages and tasks.
13. See real verification and review.
14. See real files, changes, artifacts and test proof.
15. Approve or reject risky actions.
16. Stop a real run.
17. Resume safely after a restart or crash.
18. Continue projects later without losing context.
19. Use the same professional black-and-white Forge theme everywhere.
20. Trust that every displayed operational state comes from real evidence.

The user must be able to use Forge mainly through chat.

Advanced technical information should be available through progressive disclosure, inspectors, Mission Control, activity logs and proof views.

==================================================
CANONICAL PROJECTS ROOT
==================================================

All managed Forge projects must live under:

~/Documents/ForgeProjects/

On Windows this must resolve safely to the authenticated user’s real Documents directory, equivalent to:

C:\Users\<CURRENT_USER>\Documents\ForgeProjects\

Examples:

~/Documents/ForgeProjects/webauto
~/Documents/ForgeProjects/100apps

Do not hardcode:

- a username;
- a drive letter;
- an English-only Documents path;
- a specific Windows installation layout.

Resolve the actual user Documents directory safely.

If `ForgeProjects` does not exist:

1. Resolve the canonical Documents directory.
2. Confirm it belongs to the current user.
3. Confirm the new folder remains below the user profile.
4. Ensure no conflicting file exists at that path.
5. Create the directory.
6. Record the action in the local audit ledger.

Every project created by the Workspace must be placed under:

~/Documents/ForgeProjects/<sanitized-project-slug>/

Security rules:

- reject `../` traversal;
- reject absolute-path injection;
- reject null bytes;
- reject Windows reserved names;
- reject invalid trailing dots or spaces;
- prevent Unicode-confusable collisions;
- prevent duplicate canonical paths;
- prevent symlink escape;
- prevent junction escape;
- prevent creation outside the trusted root;
- use stable project IDs independently from display names;
- do not derive identity solely from a folder name;
- allow display-name changes without changing project identity.

==================================================
NO DIRECT ANTHROPIC API
==================================================

The connected Workspace must use the locally authenticated Claude Code environment.

Required:

USES_ANTHROPIC_API=false
REQUIRES_ANTHROPIC_API_KEY=false
USES_LOCAL_CLAUDE_CODE=true

Do not:

- call the Anthropic Messages API;
- ask for an Anthropic API key;
- add an API-key form for Claude;
- store Claude credentials in this project;
- copy credentials from system storage;
- create a separate per-message billing architecture;
- automate or scrape the Visual Studio Code user interface.

Use the supported local Claude Code executable.

The local Claude Code session is already authenticated through the user’s subscription.

The Workspace may invoke the supported local Claude Code CLI from a secure local bridge.

==================================================
CLAUDE CODE COMPATIBILITY AUDIT
==================================================

Before implementation:

1. Locate the installed Claude Code executable.
2. Record its canonical executable path.
3. Record the installed version.
4. Inspect current CLI help.
5. Inspect supported input and output formats.
6. Inspect supported session/resume behavior.
7. Inspect supported permission options.
8. Inspect supported model and effort settings.
9. Inspect available usage or status telemetry.
10. Run a safe health check.
11. Confirm that the local session is authenticated.
12. Do not expose authentication secrets.

Do not assume documentation for an older Claude Code version matches the installed version.

Build a version-aware compatibility adapter.

Possible supported modes to evaluate include:

- non-interactive prompt execution;
- stream JSON input;
- stream JSON output;
- verbose events;
- session creation;
- session resume;
- continue conversation;
- allowed tools;
- disallowed tools;
- permission modes;
- maximum turns;
- cancellation.

Use only flags confirmed by the installed version.

Do not globally use unrestricted or dangerous permission modes.

Do not use `--dangerously-skip-permissions` as a default.

==================================================
NON-NEGOTIABLE TRUTHFULNESS
==================================================

No operational status may be simulated in production mode.

The following may only appear when backed by a real runtime fact:

- STARTING
- RUNNING
- STREAMING
- WAITING
- WAITING_FOR_PERMISSION
- VERIFYING
- REVIEWING
- REPAIRING
- RETRYING
- STOPPING
- COMPLETED
- FAILED
- BLOCKED
- CANCELLED
- INTERRUPTED
- DISCONNECTED
- RECOVERING
- DEGRADED

A `RUNNING` state requires:

- a real run ID;
- a real project ID;
- a real active process or Forge task;
- a real start timestamp;
- a recent heartbeat or runtime event;
- a real task or operation association.

A `COMPLETED` state requires:

- real process completion;
- required checks completed;
- persisted output;
- matching final event;
- matching run ID;
- required proof;
- required verification verdict.

A `FAILED` state requires:

- a real failing process;
- a failing test;
- a failed gate;
- or a real error event;
- with captured evidence.

A `VERIFYING` state requires:

- a real Verify Agent assignment;
- a real verification start event;
- real evidence being inspected.

A `REVIEWING` state requires:

- a real Review Boss task;
- a real review start event;
- real work-package evidence.

Never infer success from:

- a file existing;
- a process starting;
- a positive phrase in stdout;
- a single event name;
- an optimistic frontend update;
- an earlier run;
- a mock fixture;
- a command that returned output without a checked exit code.

After this phase:

CONNECTED_TO_FORGE=true
CONNECTED_TO_CLAUDE_CODE=true
USES_REAL_PROJECTS=true
USES_REAL_AGENTS=true
USES_REAL_COMMANDS=true
USES_MOCK_DATA=false

These declarations must be mechanically verified.

==================================================
REMOVE PRODUCTION MOCK DATA
==================================================

The current visual prototype contains example data and `EXAMPLE` labels.

Production mode must not load:

- example projects;
- example conversations;
- example agents;
- example tasks;
- example runtime data;
- example test results;
- example artifacts;
- fake usage values;
- simulated progress percentages.

Development fixtures may remain only under an explicit fixture/test directory.

They must be activated only by an explicit test mode.

Production startup must fail or warn clearly if fixture data is accidentally enabled.

Add a build/runtime assertion:

PRODUCTION_MOCK_DATA_ALLOWED=false

==================================================
LOCAL-ONLY ARCHITECTURE
==================================================

Use this architecture:

Forge Workspace frontend
↕
typed local real-time transport
↕
secure Forge Workspace local bridge
↕
real Forge runtime and local Claude Code CLI
↕
projects under ~/Documents/ForgeProjects

The bridge must bind by default only to:

127.0.0.1

Do not bind to:

0.0.0.0

Do not:

- expose the service to Wi-Fi;
- open router ports;
- create a public tunnel;
- deploy to Vercel;
- deploy to Cloudflare;
- deploy to Netlify;
- expose remote command execution;
- enable remote access.

Required current configuration:

LAN_MODE=false
REMOTE_ACCESS=false
BIND_ADDRESS=127.0.0.1

A future LAN mode may be documented and scaffolded as disabled configuration only.

Activating LAN mode later requires separate owner approval and a security review.

==================================================
SECURE LOCAL BRIDGE
==================================================

Do not spawn commands directly from the browser.

Build or adapt a secure local bridge responsible for:

- project discovery;
- project creation;
- project registry;
- conversations;
- Claude session mapping;
- Claude Code process lifecycle;
- Forge runtime lifecycle;
- task execution;
- event normalization;
- event persistence;
- real-time event delivery;
- usage telemetry;
- file upload staging;
- file validation;
- file indexing;
- permissions;
- approvals;
- cancellation;
- retries;
- checkpoints;
- artifacts;
- health checks;
- diagnostics;
- crash recovery;
- graceful shutdown.

The browser may never receive unrestricted filesystem or shell access.

Do not create a generic endpoint such as:

POST /execute
{
  "command": "arbitrary user-controlled shell text"
}

Use typed allowlisted operations.

Examples:

- createProject
- listProjects
- openProject
- createConversation
- sendMessage
- stopRun
- resumeSession
- attachFile
- removeAttachment
- approveAction
- denyAction
- inspectArtifact
- runApprovedTest
- getUsageState
- getHealth

Every operation must validate:

- project ID;
- canonical path;
- user intent;
- permission;
- input schema;
- operation state;
- audit metadata.

==================================================
REAL-TIME TRANSPORT
==================================================

Use the best reliable existing real-time mechanism in the repository.

Prefer:

- WebSocket for bidirectional realtime updates and approval interactions;

or:

- SSE for event delivery plus a separate typed command channel.

Do not create duplicate event systems when Forge already has a reliable stream.

Every event must contain:

{
  "eventId": "unique-id",
  "schemaVersion": 1,
  "sequence": 1,
  "timestamp": "ISO-8601",
  "projectId": "project-id",
  "runId": "run-id-or-null",
  "sessionId": "claude-session-id-or-null",
  "conversationId": "conversation-id-or-null",
  "taskId": "task-id-or-null",
  "agentId": "agent-id-or-null",
  "source": "forge|claude-code|bridge|test|user",
  "type": "event.type",
  "status": "optional-status",
  "payload": {},
  "evidenceRefs": []
}

Requirements:

- monotonic sequence per run or stream;
- unique event IDs;
- persistent replayable events;
- duplicate suppression;
- gap detection;
- out-of-order detection;
- heartbeat;
- reconnect;
- backpressure;
- stale-state detection;
- payload-version validation;
- source attribution;
- project/run isolation.

When a sequence gap occurs:

1. Display `DEGRADED`.
2. Request replay from the last confirmed sequence.
3. Reconcile persisted state.
4. Verify the missing range.
5. Remove the warning only after successful reconciliation.

No silent event loss.

==================================================
FLUIDITY AND LATENCY
==================================================

Do not claim mathematically zero delay.

Build for immediate and fluid local feedback.

Target values:

- runtime event to bridge ingestion: p95 below 25 ms;
- bridge to browser delivery: p95 below 50 ms;
- event to visible UI update: p95 below 100 ms;
- send click to accepted state: p95 below 100 ms;
- cached project switch: p95 below 150 ms;
- no unexplained main-thread freeze above 100 ms;
- streamed text updates smoothly without waiting for full completion.

Measure actual:

- ingestion latency;
- delivery latency;
- render latency;
- event backlog;
- dropped frames;
- reconnect delay;
- stream chunk rate.

Do not claim the targets were achieved unless measurements prove them.

Optimize:

- event batching;
- React state updates;
- large-list virtualization;
- graph rendering;
- Markdown rendering;
- file-tree rendering;
- conversation rendering;
- selector granularity.

Do not batch streaming so aggressively that the interface feels delayed.

==================================================
LIVE CLAUDE USAGE ABOVE THE CHAT
==================================================

Add a real live usage/status bar above the chat.

It must update from real local Claude Code and Forge runtime information.

Possible fields:

- Claude Code connection state;
- current model;
- current effort or mode;
- current session ID;
- active project;
- current conversation;
- elapsed session time;
- current turn count;
- requests or turns in the current session;
- current context usage;
- context window maximum where known;
- percentage of context used;
- input tokens where provided;
- output tokens where provided;
- cache/read/write tokens where provided;
- total tokens for current run;
- total tokens for current conversation;
- compaction count;
- last compaction time;
- active tools;
- active agent;
- active skill;
- current run state;
- last heartbeat;
- recent event latency;
- current queue state;
- rate-limit or plan warning when genuinely exposed.

The bar should be compact and professional.

Example desktop structure:

Claude Code · Connected
Model: [real model]
Context: [used] / [maximum] · [percentage]
Session: [elapsed]
Run: [real status]
Agent: [real agent]
Skill: [real skill or none]
Latency: [measured value]

On mobile, collapse it into a compact summary that can expand.

==================================================
USAGE HONESTY
==================================================

Do not invent subscription usage.

Distinguish these categories:

EXACT
- directly provided by Claude Code runtime or supported command output.

DERIVED
- calculated from verified exact event data.

ESTIMATED
- locally estimated from text or tokenizer logic.

UNAVAILABLE
- not exposed by the installed Claude Code version or subscription.

Every usage field must internally include:

- value;
- unit;
- source;
- accuracy class;
- updated timestamp.

Example:

{
  "name": "contextTokensUsed",
  "value": 48231,
  "source": "claude-code-stream-event",
  "accuracy": "EXACT",
  "updatedAt": "..."
}

If Claude Code does not expose total subscription quota or plan usage, display:

“Plan usage is not exposed by the local Claude Code runtime.”

Do not show fake:

- remaining subscription percentage;
- exact weekly usage;
- exact account spending;
- exact reset time;
- exact message allowance.

Only show those when real verified telemetry exists.

==================================================
USAGE UPDATE BEHAVIOR
==================================================

The usage bar must update when:

- a session starts;
- a model is selected;
- a message is accepted;
- input usage becomes available;
- output streams;
- tools run;
- context changes;
- compaction happens;
- a turn ends;
- a session resumes;
- a session disconnects;
- a run is stopped;
- telemetry becomes stale.

Requirements:

- no full-page refresh;
- no polling faster than necessary;
- prefer event-driven updates;
- show stale indicator if no update is received;
- do not reset counts during a temporary reconnect;
- preserve conversation totals after refresh;
- keep per-run, per-conversation and per-project totals separate;
- do not mix usage from different sessions.

Add views for:

- current run;
- current conversation;
- current project;
- current local day;
- historical local sessions where verified.

Do not create account-level claims from project-level data.

==================================================
USAGE DETAILS POPOVER
==================================================

Clicking the usage bar should open a detailed panel containing:

- session metadata;
- exact versus estimated labels;
- input token count;
- output token count;
- cache token count where available;
- total context;
- context percentage;
- current model;
- current mode;
- start time;
- elapsed time;
- turn count;
- tool-call count;
- agent count;
- skill-use count;
- errors;
- retries;
- compactions;
- average event latency;
- last telemetry update;
- data source explanation.

Include a small local history graph only when based on real stored data.

Do not show decorative fake chart values.

==================================================
USAGE ALERTS
==================================================

Add configurable local warnings for:

- context usage above 70%;
- context usage above 85%;
- context usage above 95%;
- high event latency;
- stale Claude process;
- too many retries;
- repeated compaction;
- session approaching configured maximum duration;
- unexpectedly high tool activity;
- disconnected local bridge.

Warnings should not interrupt normal work unless risk is high.

Do not claim that context exhaustion is guaranteed at a specific moment.

Offer safe actions:

- compact or summarize context;
- create a checkpoint;
- start a new conversation;
- preserve current project state;
- review large attachments;
- inspect tool activity.

Do not automatically discard conversation state.

==================================================
REAL FILE UPLOADS AND ATTACHMENTS
==================================================

Replace the example attachment menu with real file attachments.

Support:

- file picker;
- drag and drop;
- clipboard paste;
- multi-file selection;
- removal before sending;
- file previews;
- image previews;
- attachment progress;
- upload failure state;
- duplicate detection;
- retry;
- attachment metadata;
- accessible keyboard operation.

Supported types should be determined safely and may include:

- plain text;
- Markdown;
- JSON;
- YAML;
- CSV;
- source-code files;
- common document formats;
- PDF;
- PNG;
- JPEG;
- WEBP;
- SVG only with strict safe handling;
- ZIP only through safe inspection rules.

Do not claim support for a type that is not actually processed.

==================================================
ATTACHMENT STORAGE
==================================================

Files must not be stored in a global uncontrolled folder.

Use a project-scoped staging location such as:

<project-root>/.forge/attachments/<conversation-id>/<attachment-id>/

Or adapt to the existing Forge storage architecture.

Each attachment record must contain:

- attachment ID;
- project ID;
- conversation ID;
- original filename;
- sanitized stored filename;
- canonical stored path;
- media type;
- detected media type;
- size;
- hash;
- created timestamp;
- uploader source;
- preview availability;
- processing state;
- security result;
- Claude accessibility state;
- deletion state.

Do not trust the filename extension alone.

Validate file signatures where practical.

==================================================
ATTACHMENT SECURITY
==================================================

Required protections:

- filename sanitization;
- path traversal prevention;
- symlink escape prevention;
- canonical project boundary check;
- MIME validation;
- extension/signature mismatch detection;
- file-size limits;
- total attachment-size limit per message;
- total project attachment quota;
- decompression limit;
- ZIP bomb protection;
- nested archive limit;
- executable detection;
- script and binary warning;
- SVG sanitization or safe non-executing rendering;
- HTML preview sandbox;
- Markdown sanitization;
- image metadata handling;
- secret-pattern detection;
- malicious terminal escape handling;
- no automatic execution;
- no automatic opening in a shell.

Do not execute any uploaded file merely because it is attached.

Do not automatically install dependencies from an uploaded project.

Do not automatically run uploaded scripts.

Executable or high-risk files must be:

- rejected;
- quarantined;
- or shown as metadata only;
- depending on the security policy.

==================================================
ATTACHMENT PROCESSING
==================================================

Use a real state machine:

SELECTED
→ VALIDATING
→ HASHING
→ STAGING
→ INDEXING
→ READY

Alternative states:

REJECTED
QUARANTINED
FAILED
REMOVED

A message may reference only attachments in `READY` state.

If attachment processing fails:

- show the real reason;
- keep the draft text;
- allow retry;
- do not silently send without the attachment.

==================================================
CLAUDE CODE FILE ACCESS
==================================================

When a message with attachments is sent:

1. Resolve the selected project.
2. Validate attachment ownership.
3. Confirm each attachment is ready.
4. Provide Claude Code access through safe project-local file paths or an approved context mechanism.
5. Include a structured attachment manifest.
6. Do not embed huge binary contents directly in prompt text.
7. Let Claude inspect files using approved local tools.
8. Record which files were actually accessed.
9. Record attachment references in conversation history.
10. Keep project boundaries enforced.

The message should communicate:

- attachment names;
- media types;
- safe local paths;
- requested user action;
- any processing limitations.

Do not claim Claude inspected a file unless a real file-read/tool event proves it.

==================================================
ATTACHMENT USER EXPERIENCE
==================================================

The chat composer must show:

- selected files;
- thumbnails where supported;
- file type;
- file size;
- validation state;
- remove action;
- retry action;
- warning state;
- total attachment size;
- attachment count.

Clicking a file opens a safe preview.

The attachment menu should contain real options, for example:

- Upload files
- Upload folder where supported and approved
- Paste from clipboard
- Attach project file
- Recent attachments
- Clear attachments

“Attach project file” must allow browsing only within the selected project.

Do not expose unrelated files from the user’s computer.

==================================================
PROJECT FILE PICKER
==================================================

Create a safe project-local file picker.

It may browse:

- project files;
- existing project artifacts;
- previous conversation attachments.

It may not browse outside the project root.

Features:

- search;
- filter by type;
- sort;
- recent files;
- changed files;
- multi-select;
- file preview;
- path breadcrumb.

Reject:

- traversal;
- symlink escape;
- hidden secret locations;
- `.git` internals where not necessary;
- credential files;
- private environment files by default.

Sensitive files should show:

“Restricted file — explicit owner approval required.”

==================================================
PROJECT REGISTRY
==================================================

Create or adapt one canonical project registry.

Each project record must include:

- stable project ID;
- display name;
- sanitized slug;
- canonical path;
- relative path from projects root;
- project type;
- created timestamp;
- updated timestamp;
- Forge version;
- template version;
- Git state;
- Claude session IDs;
- conversation IDs;
- active run IDs;
- archived state;
- health state;
- last Forge Doctor result;
- metadata schema version.

No subsystem may reconstruct project paths from display names.

The following must all use this registry:

- dashboard;
- bridge;
- project scanner;
- new-project flow;
- conversation service;
- Claude launcher;
- Forge runtime;
- tasks;
- agents;
- artifacts;
- attachments;
- receipts;
- backups;
- doctor;
- tests;
- activity;
- archive/delete.

==================================================
NEW PROJECT CREATION
==================================================

The New Project function must genuinely work.

Flow:

1. Click New Project.
2. Enter project name.
3. Optionally enter description.
4. Select project type or let Forge recommend one.
5. Validate name.
6. Validate canonical path.
7. Detect collisions.
8. Create stable project ID.
9. Create project folder under `~/Documents/ForgeProjects`.
10. Initialize Forge project structure.
11. Create or safely merge project `CLAUDE.md`.
12. Apply relevant global Forge instructions.
13. Initialize local project registry.
14. Initialize local Git.
15. Create baseline commit.
16. Run Forge Doctor.
17. Store creation receipt.
18. Display real result.
19. Open the new project.
20. Start a real Claude Code conversation in that project.

Do not:

- create a remote repository;
- publish code;
- push to GitHub;
- deploy automatically.

==================================================
GIT BASELINE AND SAFETY
==================================================

The prototype report says the current Workspace has no Git repository.

Before major connected implementation:

1. Verify whether Git is absent.
2. Create a complete external backup.
3. Create a file-hash manifest.
4. Initialize local Git.
5. Add a correct `.gitignore`.
6. Exclude:
   - credentials;
   - local secrets;
   - runtime databases where appropriate;
   - node_modules;
   - transient logs;
   - caches;
   - uploaded sensitive attachments;
   - local session data where appropriate.
7. Create a baseline commit of the approved prototype.
8. Create a local tag or clear baseline reference.
9. Do not configure a remote.
10. Do not publish or push.

Create recoverable commits for major work packages.

==================================================
CONVERSATIONS AND SESSION PERSISTENCE
==================================================

Each conversation must map to:

- project ID;
- conversation ID;
- Claude Code session ID;
- title;
- created timestamp;
- updated timestamp;
- message references;
- event references;
- attachment references;
- usage totals;
- active run;
- archived state;
- last confirmed event sequence.

Requirements:

- start new conversation;
- resume previous conversation;
- reopen after refresh;
- reopen after bridge restart;
- reopen after computer restart;
- keep sessions separated by project;
- prevent project A from resuming project B’s session;
- preserve streamed output;
- preserve attachment references;
- preserve usage history;
- detect missing or invalid Claude sessions;
- display truthful recovery status;
- never fabricate historical messages.

Use robust local persistence.

Document:

- schema;
- migrations;
- transactions;
- locking;
- backup;
- corruption recovery;
- deletion;
- export/import.

==================================================
REAL FORGE ORCHESTRATION
==================================================

Connect the dashboard to the real existing Forge orchestration.

A normal user request should trigger the real flow:

User request
→ intake/router
→ Boss
→ Head Chef
→ work packages
→ tasks
→ relevant subagents
→ relevant skills
→ implementation
→ self-review
→ Verify Agent
→ tests
→ Review Boss
→ repair loop where necessary
→ retest
→ final output

The dashboard must show only agents genuinely activated.

For every agent activation store:

- agent ID;
- role;
- task;
- project;
- run;
- start event;
- end event;
- real model route;
- tool permissions;
- attached skills;
- output;
- verification state.

The dashboard must show only skills genuinely attached or used.

For every skill-use record store:

- skill ID;
- version or hash;
- reason selected;
- task;
- load timestamp;
- actual use evidence;
- result;
- provenance.

No fake agent activity.

No fake skills.

No placeholder percentages in production mode.

==================================================
VERIFY AGENT
==================================================

Every completed subagent task must receive an independent Verify Agent verdict.

Flow:

Subagent assigned
→ task starts
→ output produced
→ self-review
→ Verify Agent receives original assignment and evidence
→ independent verification
→ verdict
→ repair when rejected
→ re-verification
→ work becomes eligible for Review Boss

Verdicts:

- VERIFIED PASS
- VERIFIED PASS WITH LIMITATIONS
- REJECTED
- BLOCKED
- INSUFFICIENT EVIDENCE

A subagent may not approve its own task.

A task requiring verification may not display fully completed until the verdict exists.

Use `UNVERIFIED` when output exists but verification is missing.

==================================================
FILES, DIFFS AND ARTIFACTS
==================================================

Connect Files and Artifacts to real project data.

File operations must respect project boundaries.

Required features:

- project file tree;
- file search;
- recent files;
- changed files;
- actual file preview;
- actual diff preview;
- artifact gallery;
- real screenshot preview;
- report preview;
- log preview;
- download where safe;
- hash and metadata;
- link to run/task/agent.

Every artifact record:

- artifact ID;
- project ID;
- run ID;
- task ID;
- creator agent;
- canonical path;
- media type;
- size;
- hash;
- created timestamp;
- verification status;
- proof references.

Missing artifacts must show:

MISSING ARTIFACT

Do not generate a fake placeholder and present it as the real artifact.

==================================================
TESTS AND PROOF
==================================================

The Tests and Proof view must display real executions.

A successful test gate requires:

- the command truly ran;
- start and end timestamps;
- exit code;
- stdout/stderr;
- duration;
- test counts where available;
- matching project and run;
- required files;
- required proof;
- successful verification.

Do not mark a command successful because stdout contains “OK” when exit code is failing.

Do not trust exit code alone when the wrapper is known to return an incorrect code.

Fix the wrapper so:

- result output;
- receipt;
- test parser;
- exit code;
- UI state

all agree.

==================================================
OWNER APPROVALS
==================================================

Owner approval is required for:

- deleting projects;
- deleting important files;
- commands outside the trusted project root;
- publishing;
- pushing;
- deployment;
- remote repository creation;
- LAN exposure;
- remote access;
- external communications;
- emails or messages;
- live database-destructive operations;
- payments;
- live service modifications;
- high-risk dependency installation;
- credential access;
- global system changes;
- broad filesystem access.

An approval request must include:

- exact action;
- requesting agent;
- project;
- command or operation;
- files/services affected;
- risk level;
- reason;
- rollback plan;
- timeout;
- approve;
- deny.

A denied or expired request may not be treated as successful.

==================================================
STATE MACHINES
==================================================

Define explicit state machines.

Run states:

CREATED
→ QUEUED
→ STARTING
→ RUNNING
→ WAITING_FOR_PERMISSION
→ VERIFYING
→ REVIEWING
→ REPAIRING
→ COMPLETED

Terminal or alternate states:

FAILED
CANCELLED
BLOCKED
INTERRUPTED
ORPHANED
RECOVERING

Disallow invalid transitions.

Examples:

- CREATED → COMPLETED without execution is invalid.
- FAILED → COMPLETED without repair and retest is invalid.
- CANCELLED → RUNNING under the same immutable attempt is invalid.
- VERIFYING → COMPLETED without verdict is invalid.
- COMPLETED → RUNNING under the same attempt is invalid.
- DISCONNECTED → COMPLETED without reconciliation is invalid.

Create state machines for:

- runs;
- tasks;
- agents;
- tests;
- permissions;
- attachments;
- Claude sessions;
- projects;
- artifacts;
- streams.

==================================================
STOP AND CANCELLATION
==================================================

The Stop button must genuinely stop the correct run.

Requirements:

- request graceful stop;
- persist `CANCEL_REQUESTED`;
- wait for confirmation;
- use timeout;
- force terminate only when required;
- terminate the intended child process tree;
- never kill unrelated Claude sessions;
- preserve partial output;
- preserve proof;
- persist `CANCELLED` only after termination is confirmed;
- allow a new run later.

Test:

- rapid start/stop;
- double stop;
- stop during stream;
- stop during tool call;
- stop during verification;
- stop after process already ended;
- stop one session while another remains active.

==================================================
CRASH RECOVERY
==================================================

Support:

- browser refresh;
- browser close/reopen;
- bridge restart;
- Claude process crash;
- Forge process crash;
- computer restart;
- local transport interruption;
- corrupt last event;
- incomplete storage write;
- locked file;
- duplicate event;
- missing event;
- out-of-order event;
- moved project folder;
- missing artifact.

Use:

- atomic writes;
- transactions;
- append-only or replayable event log;
- checkpoints;
- idempotent handlers;
- stale-run reconciliation;
- orphan detection;
- session resume;
- process reconciliation.

After restart, do not assume an earlier `RUNNING` process still exists.

Use truthful recovery states:

- INTERRUPTED
- RECOVERING
- RESUMABLE
- ORPHANED
- FAILED_RECOVERY

==================================================
GLOBAL CLIENT STATE
==================================================

All dashboard views must use one normalized real state.

Examples:

- a task becomes RUNNING once and updates Mission Control, Tasks, Chat and Agent inspector;
- a test passes once and updates Tests, Proof, Activity and run summary;
- a project is created once and appears on Home, Projects and the sidebar;
- a run is cancelled once and all views agree;
- a usage update appears in the chat bar and usage details without contradictory totals;
- an attachment removed in the composer cannot remain attached in conversation state.

Do not maintain independent fake stores per screen.

Test cross-view consistency.

==================================================
OBSERVABILITY
==================================================

Record:

- project ID;
- conversation ID;
- run ID;
- session ID;
- task ID;
- agent;
- operation;
- start/end;
- duration;
- exit code;
- event latency;
- render latency;
- queue latency;
- reconnect count;
- replay count;
- missing-event count;
- attachment processing time;
- upload size;
- Claude usage source;
- process state;
- bridge health;
- memory;
- CPU where practical;
- error class.

Do not record:

- credentials;
- authentication tokens;
- secrets;
- sensitive file contents unnecessarily.

Provide:

- local health endpoint or typed health channel;
- structured logs;
- diagnostics export;
- redaction;
- log rotation;
- health UI.

==================================================
FIX CURRENT PROTOTYPE LIMITATIONS
==================================================

Resolve the known limitations.

1. Bundle size

Current JavaScript bundle is approximately 1.23 MB because icons are dynamically looked up.

Required:

- replace whole-library dynamic icon lookup with a static icon map;
- lazy-load large screens;
- implement route splitting;
- inspect CSS size;
- define bundle budgets;
- measure before and after;
- add regression checks.

2. React-refresh warnings

Resolve avoidable warnings.

3. Sidebar footer

Fix the two-line wrapping and verify all widths.

4. Inert controls

Every production-visible control must be:

- fully functional;
- clearly disabled with an honest reason;
- or removed.

Do not keep a button that only shows a fake success toast.

5. Claude Code fixture browser

Replace it with real:

- local CLI status;
- version;
- session;
- model;
- heartbeat;
- current mode;
- usage source.

6. Example attachments

Replace with real upload and project-file attachment flows.

7. Screenshot artifacts

Show actual images when they exist.

8. Mission graph

Preserve improved zoom behavior and test:

- 10 nodes;
- 100 nodes;
- 1,000 nodes where practical;
- wide parallel graphs;
- deep chains;
- loops;
- missing events;
- delayed nodes.

==================================================
PRODUCT DISCOVERY AND ADDITIONAL FEATURES
==================================================

Do not only implement the features explicitly listed.

Perform a product-improvement discovery phase.

Inspect:

- the current prototype;
- real user workflows;
- current Forge capabilities;
- Claude Code capabilities;
- project-management needs;
- agent observability needs;
- local-first application patterns;
- developer productivity workflows;
- accessibility;
- recovery;
- security;
- speed;
- information overload.

Identify functionality that can make Forge Workspace:

- easier;
- faster;
- safer;
- more understandable;
- more reliable;
- more useful for long projects;
- more suitable for future Mac mini hosting.

Create:

product/feature-opportunity-matrix.json
product/feature-roadmap.md

Every feature candidate must include:

- feature name;
- user problem;
- proposed behavior;
- affected screens;
- dependency;
- security impact;
- complexity;
- expected value;
- local-first suitability;
- Mac mini suitability;
- test plan;
- adoption decision.

==================================================
FEATURE IDEAS TO EVALUATE
==================================================

Evaluate at least these ideas, but do not limit discovery to them:

1. Universal search across:
   - projects;
   - conversations;
   - files;
   - tasks;
   - agents;
   - artifacts;
   - proof.

2. Conversation branches:
   - branch from an earlier message;
   - preserve original branch;
   - compare outcomes.

3. Checkpoint and rewind:
   - create named checkpoint;
   - return to earlier project or conversation state;
   - show affected files.

4. Prompt library:
   - reusable prompts;
   - project templates;
   - favorite prompts;
   - slash-command discovery.

5. Project templates:
   - website;
   - n8n automation;
   - API;
   - desktop app;
   - research;
   - prediction system;
   - scraping;
   - blank Forge project.

6. Diff review:
   - inspect file changes;
   - approve/reject selected changes;
   - compare before/after;
   - restore file from checkpoint.

7. Context manager:
   - show context sources;
   - attachment contribution;
   - project instruction contribution;
   - CLAUDE.md state;
   - compaction history;
   - context warnings.

8. Session health:
   - Claude status;
   - Forge bridge status;
   - stale process warning;
   - recovery suggestions.

9. Usage dashboard:
   - session totals;
   - project totals;
   - model distribution;
   - context usage;
   - tool calls;
   - actual versus estimated values.

10. Notifications:
   - task completed;
   - approval required;
   - verification rejected;
   - run failed;
   - context warning;
   - bridge disconnected.

11. Focus mode:
   - hide advanced panels;
   - chat-only mode;
   - restore workspace layout.

12. Multi-panel workspace:
   - chat and files;
   - chat and diff;
   - chat and Mission Control;
   - chat and test output.

13. Recent and pinned items:
   - projects;
   - conversations;
   - files;
   - artifacts;
   - prompts.

14. Project health:
   - Git status;
   - Forge Doctor;
   - dependency state;
   - failing tests;
   - missing documentation;
   - stale sessions.

15. Project import:
   - import existing folder from inside approved root;
   - inspect before Forge initialization;
   - do not overwrite.

16. Backup and restore:
   - project backup;
   - Workspace data backup;
   - restore preview;
   - integrity verification.

17. Export:
   - conversation export;
   - report export;
   - proof export;
   - project manifest export.

18. Local audit log:
   - approvals;
   - commands;
   - file access;
   - project creation;
   - attachment actions;
   - session activity.

19. Keyboard-first workflows:
   - command palette;
   - navigation shortcuts;
   - send/stop;
   - open files;
   - switch project.

20. Agent timeline:
   - real agent start/end;
   - dependencies;
   - verifier verdict;
   - repair loops.

21. Task dependency graph:
   - blocked-by;
   - depends-on;
   - critical path;
   - retry count.

22. Project scratchpad:
   - user notes;
   - decisions;
   - pinned requirements;
   - not automatically treated as verified facts.

23. Local notifications when browser is in background.

24. Automatic session title generation using the local Claude Code session where safe.

25. Conversation archive and restore.

26. Project archive without deletion.

27. Duplicate-project detection.

28. Recent attachment library.

29. Attachment search and reuse.

30. Safe folder upload.

31. Markdown and code preview.

32. Large-file warnings.

33. Local offline/degraded mode.

34. Theme density controls.

35. Customizable workspace layout.

36. Accessibility profiles.

37. Reduced technical detail mode.

38. Advanced operator mode.

39. Per-project permissions.

40. Per-project model/mode defaults.

==================================================
FEATURE DECISION RULES
==================================================

Features may be implemented automatically when they are:

- local;
- reversible;
- within the current project;
- non-destructive;
- safe;
- testable;
- useful;
- not dependent on unavailable credentials;
- not a public deployment;
- not LAN exposure;
- not unnecessary scope duplication.

For each candidate classify:

- IMPLEMENT NOW
- IMPLEMENT AS OPT-IN
- DESIGN FOR LATER
- REJECT
- OWNER APPROVAL REQUIRED

Do not overload the UI with every possible feature.

Use progressive disclosure.

Prefer fewer polished, connected features over many shallow controls.

Do not add a feature merely because it sounds impressive.

==================================================
SOLUTION-FIRST DEVELOPMENT
==================================================

Do not give up after one failed implementation method.

For every non-security blocker:

1. Determine the exact root cause.
2. Generate at least three safe alternatives.
3. Rank them.
4. Try the strongest alternatives.
5. Verify the result.
6. Continue independent tasks.
7. Mark blocked only after reasonable alternatives are exhausted.

A failed method does not mean the objective is impossible.

Stop only when continuing requires:

- authentication bypass;
- unsafe permissions;
- external unauthorized access;
- destructive action without approval;
- exposure of secrets;
- unresolved critical security risk.

==================================================
CAPABILITY INVENTORY
==================================================

Before ultimate testing, mechanically inventory every function.

Include:

- every route;
- every page;
- every interactive component;
- every button;
- every menu;
- every input;
- every setting;
- every keyboard shortcut;
- every command-palette action;
- every local bridge operation;
- every storage operation;
- every process operation;
- every file operation;
- every attachment operation;
- every usage field;
- every state transition;
- every event type;
- every project action;
- every conversation action;
- every session action;
- every run action;
- every agent action;
- every skill action;
- every task action;
- every artifact action;
- every proof action;
- every approval action;
- every error state;
- every responsive layout.

Create a machine-readable matrix containing:

- capability ID;
- screen;
- control;
- expected behavior;
- implementation;
- bridge handler;
- permission;
- positive test;
- negative test;
- security test;
- chaos test;
- status;
- proof.

No capability is complete without:

- real successful-path test;
- real failure-path test;
- permission/security test where relevant.

==================================================
ULTIMATE TEST PROGRAM
==================================================

Do not only test whether the app builds.

Attempt to break the entire system safely.

--------------------------------------------------
A. STATIC AND BUILD TESTS
--------------------------------------------------

Run:

- clean dependency install;
- theme generation;
- theme drift check;
- strict TypeScript;
- unused-local checks;
- ESLint;
- formatting;
- circular dependency checks;
- unused export checks;
- dependency duplication checks;
- bundle analysis;
- production build;
- local production startup;
- clean worktree verification.

--------------------------------------------------
B. UNIT TESTS
--------------------------------------------------

Test:

- path validation;
- project slug generation;
- canonical root boundaries;
- registry logic;
- state machines;
- event normalization;
- event ordering;
- duplicate suppression;
- gap detection;
- session mapping;
- status derivation;
- usage aggregation;
- usage accuracy labels;
- attachment validation;
- filename sanitization;
- MIME detection;
- hashing;
- permission rules;
- redaction;
- process results;
- cancellation;
- retry;
- checkpoints;
- artifact indexing;
- feature flags.

--------------------------------------------------
C. CONTRACT TESTS
--------------------------------------------------

Test contracts between:

- frontend and bridge;
- bridge and Forge;
- bridge and Claude Code;
- bridge and filesystem;
- bridge and attachment storage;
- Claude event stream and usage system;
- event store and UI state;
- project registry and filesystem;
- task output and Verify Agent;
- test runner and proof;
- approvals and process continuation.

Reject invalid payloads safely.

--------------------------------------------------
D. INTEGRATION TESTS
--------------------------------------------------

Use isolated temporary project roots.

Test:

- create project;
- initialize Forge;
- initialize Git;
- open conversation;
- start real Claude Code session;
- stream real harmless response;
- capture session ID;
- resume session;
- attach text file;
- attach image;
- inspect attachment;
- remove attachment;
- upload failure;
- multiple conversations;
- multiple projects;
- cancel run;
- restart bridge;
- reconnect;
- recover state;
- index artifacts;
- run harmless approved command;
- store real proof.

Do not run destructive tests against real user projects.

--------------------------------------------------
E. END-TO-END TESTS
--------------------------------------------------

Test:

1. First startup
2. Existing project discovery
3. New project creation
4. Project import
5. New chat
6. Real Claude response
7. Real streaming
8. Live usage updates
9. Usage details
10. Context warning
11. File upload
12. Image upload
13. Multi-file upload
14. Drag and drop
15. Clipboard paste
16. File removal
17. Project-file attachment
18. Real Forge mission
19. Real subagent activation
20. Real skill selection
21. Verify Agent rejection
22. Repair
23. Retest
24. Review Boss
25. Cancel
26. Resume
27. Browser refresh during run
28. Bridge restart during run
29. Computer/app restart recovery
30. Project search
31. Universal search
32. Project archive
33. Conversation archive
34. Files
35. Artifacts
36. Tests and proof
37. Activity
38. Settings
39. Theme switching
40. Mobile navigation
41. Command palette
42. Keyboard workflows
43. Empty states
44. Error states
45. Disconnected states

--------------------------------------------------
F. NEGATIVE TESTS
--------------------------------------------------

Attempt:

- blank project name;
- long project name;
- duplicate project;
- path traversal;
- absolute path;
- reserved Windows name;
- Unicode collision;
- symlink escape;
- junction escape;
- missing Claude binary;
- unauthenticated session;
- invalid session ID;
- malformed stream JSON;
- unexpected output;
- process exit 1;
- process killed;
- missing folder;
- read-only folder;
- disk-write failure;
- locked registry;
- corrupt checkpoint;
- missing artifact;
- stale WebSocket;
- duplicate event;
- out-of-order event;
- missing event;
- invalid transition;
- denied approval;
- expired approval;
- double send;
- repeated clicks;
- rapid project switching;
- zero-byte upload;
- huge file;
- wrong MIME;
- executable upload;
- malicious filename;
- ZIP bomb;
- nested archive;
- malicious SVG;
- HTML with scripts;
- Markdown XSS;
- duplicate attachment;
- attachment deleted before send;
- stale attachment ID;
- cross-project attachment ID;
- fake usage event;
- usage from wrong session;
- stale usage source.

--------------------------------------------------
G. SECURITY TESTS
--------------------------------------------------

Test:

- path traversal;
- symlink/junction escape;
- command injection;
- shell metacharacters;
- malicious project names;
- malicious filenames;
- HTML/Markdown XSS;
- SVG scripts;
- terminal escape sequences;
- prompt-injection text display;
- unrestricted command endpoint absence;
- cross-project data access;
- cross-conversation attachment access;
- secret redaction;
- environment leakage;
- log leakage;
- localhost binding;
- WebSocket origin validation;
- privilege escalation;
- dependency vulnerabilities;
- oversized payload denial;
- archive bomb;
- arbitrary file read;
- arbitrary file write;
- executable attachment handling;
- hidden-file restrictions.

Do not attack external systems.

Run locally against isolated fixtures.

--------------------------------------------------
H. CHAOS TESTS
--------------------------------------------------

Inject:

- Claude process crash;
- Forge crash;
- bridge crash;
- browser disconnect;
- bridge restart;
- delayed event;
- event burst;
- malformed event;
- duplicate event;
- out-of-order event;
- missing sequence;
- storage lock;
- corrupted storage;
- disk-write failure;
- process timeout;
- cancellation during stream;
- cancellation during tool execution;
- Verify Agent crash;
- test runner crash;
- upload interruption;
- incomplete attachment write;
- attachment hash failure;
- project folder moved;
- temporary internet loss;
- machine sleep/wake where practical.

Required invariants:

- no fake PASS;
- no lost proof;
- no cross-project contamination;
- no cross-session usage mixing;
- no silent corruption;
- no unsafe retry;
- no project-root escape;
- no duplicate execution;
- no permanently frozen UI;
- truthful degraded state;
- recoverable checkpoint;
- incomplete attachment is never sent;
- stale usage is labeled stale.

--------------------------------------------------
I. LOAD AND PERFORMANCE TESTS
--------------------------------------------------

Test:

- 100 projects;
- 1,000 projects where practical;
- 100 conversations per project;
- long conversations;
- many attachments;
- large file trees;
- many artifacts;
- large logs;
- 100-node Mission graph;
- 1,000-node graph;
- high-frequency usage events;
- rapid stream chunks;
- repeated reconnects;
- multiple queued runs;
- many UI subscribers.

Do not start an excessive number of real Claude sessions.

Use clearly labeled synthetic transport events for rendering/load tests only.

Never use synthetic data as real execution proof.

Measure:

- startup;
- project scan;
- search;
- attachment processing;
- event latency;
- usage update latency;
- render latency;
- memory;
- CPU;
- dropped frames;
- graph performance;
- bundle size;
- route load time.

--------------------------------------------------
J. VISUAL TESTS
--------------------------------------------------

Test dark, light and system modes.

Viewports:

- 1440×900
- 1280×800
- 1024×768
- 768×1024
- 430×932
- 390×844
- 360×800

Check every screen.

Specifically inspect:

- usage bar above chat;
- expanded usage panel;
- composer with one file;
- composer with many files;
- long filenames;
- upload progress;
- upload errors;
- chat streaming;
- Mission graph;
- sidebar;
- inspector;
- bottom dock;
- project creation;
- approval modal;
- disconnected state;
- context warning;
- light-theme contrast;
- mobile attachments;
- mobile usage summary.

Capture, inspect, repair and recapture.

--------------------------------------------------
K. ACCESSIBILITY TESTS
--------------------------------------------------

Test:

- keyboard-only operation;
- drag/drop alternative;
- file upload button labels;
- file removal labels;
- progress announcements;
- streaming live-region behavior;
- usage updates without excessive announcements;
- logical tab order;
- dialog focus traps;
- screen-reader names;
- contrast;
- 200% zoom;
- reduced motion;
- touch targets;
- error association;
- no color-only statuses.

--------------------------------------------------
L. MUTATION TESTS
--------------------------------------------------

Run mutation testing on:

- state machines;
- path boundaries;
- permission rules;
- proof gating;
- status derivation;
- event ordering;
- usage aggregation;
- usage source labels;
- attachment validation;
- archive limits;
- cancellation;
- redaction;
- cross-project isolation.

Report mutation score and surviving mutations.

--------------------------------------------------
M. FUZZ AND PROPERTY TESTS
--------------------------------------------------

Fuzz:

- project names;
- paths;
- filenames;
- MIME values;
- event payloads;
- event order;
- stream chunks;
- CLI output;
- Markdown;
- SVG;
- registry data;
- usage data;
- attachment manifests;
- configuration.

Properties:

- project paths always stay inside trusted root;
- attachments stay inside their project;
- duplicate events do not create duplicate effects;
- terminal states cannot transition illegally;
- a PASS always has evidence;
- project A never changes project B;
- session A usage never appears in session B;
- an attachment not READY is never sent;
- redaction never exposes registered secret patterns.

--------------------------------------------------
N. IDEMPOTENCY TESTS
--------------------------------------------------

Verify repetition does not duplicate:

- project registration;
- project initialization;
- conversation creation request;
- event ingestion;
- task completion;
- artifact indexing;
- attachment indexing;
- attachment hashing;
- proof entries;
- session resume;
- bridge recovery;
- migrations;
- sync receipts.

--------------------------------------------------
O. REAL-WORLD CERTIFICATION
--------------------------------------------------

Create an isolated certification project under a safe test root.

Give Forge a normal natural-language request.

Do not explicitly instruct it which agents or skills to activate.

Forge must:

- interpret request;
- ask necessary questions;
- create plan;
- create work packages;
- select agents;
- select skills;
- use a real attached file;
- implement a small real project;
- run tests;
- experience one controlled failure;
- repair it;
- run Verify Agent;
- run Review Boss;
- produce artifacts;
- produce final proof.

Check causality:

- intake before plan when information is missing;
- task assignment before execution;
- skill attachment before skill use;
- attachment READY before use;
- failure before repair;
- repair before retest;
- retest before PASS;
- Verify Agent before acceptance;
- Review Boss after task verification;
- final result after required gates.

A list of events is not sufficient.

==================================================
NO TEST THEATRE
==================================================

Reject tests that only prove:

- a file exists;
- a component renders;
- a process starts;
- a label appears;
- an event name exists;
- a command exits zero without behavior inspection;
- an artifact path exists without content/hash validation;
- a screenshot exists without visual inspection;
- a mock integration works while real integration remains untested.

High-risk claims require real behavioral proof.

==================================================
AUTONOMOUS REPAIR LOOP
==================================================

When a test fails:

failure
→ root-cause classification
→ repair task
→ appropriate agent
→ focused implementation
→ regression test
→ Verify Agent
→ rerun failed test
→ rerun affected suite
→ continue full program

Fix safe local defects instead of merely listing them.

Critical or high-severity unresolved defects block final certification.

==================================================
WORK PACKAGES
==================================================

Proceed automatically.

Do not ask me to type “continue”.

WP0 — BACKUP, GIT AND BASELINE

- verify current report;
- create external backup;
- hash files;
- initialize local Git;
- baseline commit;
- inventory routes and controls;
- record current tests;
- create rollback plan.

WP1 — ARCHITECTURE AND CONTRACTS

- audit Forge;
- audit Claude Code;
- define local bridge;
- define schemas;
- define state machines;
- define storage;
- define project registry;
- define usage telemetry;
- define attachment architecture;
- define security boundaries;
- define performance targets.

WP2 — PROJECT ROOT AND REGISTRY

- implement ForgeProjects root;
- discover projects;
- import safe projects;
- create projects;
- initialize Git;
- initialize Forge;
- doctor receipts;
- registry tests.

WP3 — LOCAL CLAUDE CODE BRIDGE

- executable detection;
- version adapter;
- authentication health;
- process manager;
- real streaming;
- session creation;
- session resume;
- cancellation;
- permissions;
- stderr;
- crash handling.

WP4 — REAL-TIME EVENTS AND USAGE

- normalized events;
- persistence;
- replay;
- heartbeat;
- gap recovery;
- live usage fields;
- accuracy labels;
- usage history;
- stale handling;
- latency metrics;
- usage bar UI.

WP5 — FILE UPLOADS AND ATTACHMENTS

- file picker;
- drag/drop;
- clipboard;
- project-file picker;
- validation;
- hashing;
- staging;
- security;
- previews;
- Claude access;
- attachment history;
- failure/retry;
- cleanup.

WP6 — CONVERSATIONS AND PERSISTENCE

- real chats;
- sessions;
- history;
- attachments;
- usage totals;
- refresh recovery;
- restart recovery;
- multi-project isolation.

WP7 — REAL FORGE ORCHESTRATION

- Boss;
- Head Chef;
- work packages;
- subagents;
- skills;
- Verify Agent;
- tests;
- Review Boss;
- repair loops;
- proof.

WP8 — CONNECT ALL UI

- Home;
- Projects;
- Overview;
- Chat;
- usage bar;
- attachments;
- Mission Control;
- Agents;
- Tasks;
- Files;
- Artifacts;
- Tests;
- Proof;
- Activity;
- Settings;
- command palette;
- inspector;
- dock.

Remove all fake production controls.

WP9 — PRODUCT IMPROVEMENT DISCOVERY

- identify additional features;
- score them;
- implement safe high-value additions;
- keep risky additions owner-gated;
- update roadmap;
- verify no UI overload.

WP10 — SECURITY, RECOVERY AND PERMISSIONS

- owner approvals;
- filesystem boundaries;
- attachment security;
- redaction;
- process control;
- checkpoints;
- crash recovery;
- backup/restore;
- idempotency.

WP11 — ULTIMATE TESTING

- static;
- unit;
- contract;
- integration;
- E2E;
- negative;
- security;
- chaos;
- load;
- performance;
- visual;
- accessibility;
- mutation;
- fuzz;
- properties;
- idempotency;
- real certification.

WP12 — OPTIMIZATION AND FINAL CERTIFICATION

- repair critical/high defects;
- resolve warnings;
- reduce bundle;
- optimize realtime rendering;
- clean-worktree verification;
- final certification;
- final screenshots;
- Mac mini migration plan;
- final report.

==================================================
VERIFY AGENT AFTER EVERY TASK
==================================================

Every implementation and testing task requires independent Verify Agent review.

Verify Agent checks:

- requirement;
- actual diff;
- source files;
- command;
- output;
- exit code;
- events;
- proof;
- security;
- state correctness;
- usage truthfulness;
- attachment truthfulness;
- rollback.

The task agent may not approve itself.

Review Boss reviews the complete work package after task-level verification.

==================================================
REQUIRED LOCAL COMMANDS
==================================================

Implement working commands for:

- development startup;
- local production startup;
- complete verification;
- integration tests;
- E2E tests;
- security tests;
- chaos tests;
- mutation tests;
- screenshots;
- bundle analysis;
- ultimate certification;
- backup;
- restore;
- doctor.

Determine exact command names from the repository and implement them.

Do not only document the commands.

Run them and store results.

==================================================
CURRENT COMPUTER FIRST
==================================================

Optimize for this Windows computer first.

Requirements:

- one-command local startup;
- graceful shutdown;
- no orphan Claude processes;
- no unnecessary administrator privileges;
- spaces in paths;
- Unicode in paths;
- stable restarts;
- local-only binding;
- clear diagnostics.

==================================================
FUTURE MAC MINI READINESS
==================================================

Prepare but do not activate Mac mini hosting.

Support:

- cross-platform paths;
- Windows and macOS process adapters;
- no hardcoded shell syntax;
- portable storage;
- portable migrations;
- backup export;
- project transfer;
- local service startup documentation.

Create a future design document for:

- Mac mini service;
- launch on boot;
- encrypted local storage;
- private LAN;
- login/authentication;
- HTTPS;
- firewall;
- hostname;
- backups;
- remote shutdown;
- rate limits;
- session isolation.

Keep:

LAN_MODE=false
REMOTE_ACCESS=false

==================================================
FINAL ACCEPTANCE CRITERIA
==================================================

The Workspace is complete only when:

- the approved prototype is preserved in Git;
- backup and rollback exist;
- real projects use `~/Documents/ForgeProjects`;
- new-project creation works;
- each new project gets Forge initialization and local Git;
- Claude Code uses the locally authenticated CLI;
- no Anthropic API key is used;
- real conversations persist;
- Claude sessions resume correctly;
- output streams live;
- the usage bar shows real or honestly labeled telemetry;
- usage values never mix between sessions;
- unsupported plan usage is shown as unavailable;
- file uploads work;
- attachments are securely staged;
- unsafe files are rejected or quarantined;
- attachments are never automatically executed;
- Claude file access is recorded;
- all screens share one synchronized real state;
- no production mock data remains;
- no fake running/completed states remain;
- agents appear only when genuinely activated;
- skills appear only when genuinely used;
- Verify Agent checks every subagent task;
- tests represent real commands;
- cancellation works;
- crash recovery works;
- bridge restart recovery works;
- filesystem boundaries hold;
- localhost binding is verified;
- event latency is measured;
- usage latency is measured;
- all visible controls work or are honestly disabled;
- product improvements were researched;
- safe valuable improvements were implemented;
- capability matrix is complete;
- positive and negative tests exist;
- security tests exist;
- chaos tests exist;
- mutation tests exist;
- fuzz/property tests exist;
- idempotency tests exist;
- real certification exists;
- all critical and high defects are fixed;
- complete regression is green;
- Mac mini/LAN remains documented but disabled.

==================================================
FINAL REPORT
==================================================

Produce a detailed report containing:

1. Executive summary
2. Architecture
3. Files changed
4. Git baseline
5. Backup and rollback
6. Projects root
7. Project registry
8. New-project flow
9. Claude Code installation and version
10. Local Claude connection
11. Session persistence
12. Conversation persistence
13. Real-time event system
14. Latency measurements
15. Live usage implementation
16. Exact/derived/estimated/unavailable usage fields
17. Usage history
18. Context warnings
19. File-upload implementation
20. Attachment security
21. Attachment tests
22. Forge orchestration
23. Agent proof
24. Skill proof
25. Verify Agent proof
26. Review Boss proof
27. UI integration
28. Product-improvement research
29. Features implemented
30. Features deferred
31. Permission model
32. Security boundaries
33. Recovery behavior
34. Capability-matrix coverage
35. Unit tests
36. Contract tests
37. Integration tests
38. E2E tests
39. Negative tests
40. Security tests
41. Chaos tests
42. Load tests
43. Performance tests
44. Visual tests
45. Accessibility tests
46. Mutation score
47. Fuzz/property tests
48. Idempotency tests
49. Real-world certification
50. Defects found
51. Defects fixed
52. Remaining limitations
53. Bundle-size result
54. Windows run instructions
55. Backup/restore instructions
56. Future Mac mini plan
57. Future LAN security plan
58. Exact final verdict

Final verdict:

- CERTIFIED FOR LOCAL USE
- CERTIFIED WITH LIMITATIONS
- NOT CERTIFIED

Do not issue `CERTIFIED FOR LOCAL USE` while a critical or high-severity defect remains.

==================================================
FINAL RUNTIME DECLARATIONS
==================================================

Mechanically verify:

CONNECTED_TO_FORGE=true
CONNECTED_TO_CLAUDE_CODE=true
USES_ANTHROPIC_API=false
REQUIRES_ANTHROPIC_API_KEY=false
USES_REAL_PROJECTS=true
USES_REAL_AGENTS=true
USES_REAL_COMMANDS=true
USES_MOCK_DATA=false
USES_REAL_USAGE_TELEMETRY=true
SUPPORTS_FILE_ATTACHMENTS=true
PROJECTS_ROOT=~/Documents/ForgeProjects
LAN_MODE=false
REMOTE_ACCESS=false
BIND_ADDRESS=127.0.0.1

Start with WP0.

Continue autonomously through WP12.

Do not stop after connecting Claude Code.

Do not stop after implementing uploads.

Do not stop after the first passing test suite.

Connect every real capability, add the live usage interface, implement secure attachments, discover additional high-value features, try to break the system safely, repair defects, rerun affected suites and finish only with an honest evidence-backed certification verdict.